# 课表插件教程：历史材料

> 更新时间：2026-09-23
> 状态：旧 v1-v3 课表/受管进程示例，不能作为新插件脚手架。

本页曾以 `data/plugins`、受管进程和 v2 Manifest 讲解课表助手。当前插件源码必须自包含在 `plugins/<key>/`，并以校验后的发布包、隔离 UI、Bridge 和三层授权运行。旧示例不具备这些边界。

需要维护旧课表资料时可保留本链接；开始新开发请使用仓库实际实现 [PDF Viewer](/plugins/pdf-viewer-tutorial) 作为端到端模板。
