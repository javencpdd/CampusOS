# 待安装工具清单

> 生成时间：2024-06-24
> 系统：Ubuntu 22.04 LTS (amd64)

---

## 已安装 ✅

| 工具 | 版本 | 状态 |
|:---|:---|:---|
| Git | 系统自带 | ✅ 已安装 |
| Make | 系统自带 | ✅ 已安装 |
| Go | 1.22.5 | ✅ 已安装 |
| Go Proxy | https://goproxy.cn,direct | ✅ 已配置 |
| Node.js | 20.20.2 | ✅ 已安装 |
| Docker | 29.1.3 | ✅ 已安装 |
| Docker Compose | 2.40.3 | ✅ 已安装 |
| gopls | v0.22.0 | ✅ 已安装 |
| Delve (dlv) | — | ✅ 已安装 |
| mockgen | — | ✅ 已安装 |
| protoc | — | ✅ 已安装 |
| protoc-gen-go | — | ✅ 已安装 |
| protoc-gen-go-grpc | — | ✅ 已安装 |
| NATS Server | — | ✅ 已安装 |
| NATS CLI | — | ✅ 已安装 |
| MinIO | — | ✅ 已安装 |
| MinIO Client (mc) | — | ✅ 已安装 |
| PostgreSQL Client (psql) | — | ✅ 已安装 |
| Redis CLI | — | ✅ 已安装 |

---

## 未安装 ❌（共 5 项）

### 1. pnpm（前端包管理器）

```bash
npm install -g pnpm@8
pnpm config set registry https://registry.npmmirror.com
```

### 2. golangci-lint（Go 代码静态检查）

```bash
curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin
```

### 3. Air（Go 热重载工具）

```bash
go install github.com/air-verse/air@latest
```

### 4. Swag（Swagger 文档生成）

```bash
go install github.com/swaggo/swag/cmd/swag@latest
```

### 5. buf（Protobuf 管理工具）

```bash
BUF_VERSION="1.28.1"
curl -sSL "https://github.com/bufbuild/buf/releases/download/v${BUF_VERSION}/buf-Linux-x86_64" -o /tmp/buf
sudo install -m 755 /tmp/buf /usr/local/bin/buf
rm /tmp/buf
```

---

## 一键安装全部缺失工具

复制以下命令一次性执行：

```bash
# 1. pnpm
npm install -g pnpm@8
pnpm config set registry https://registry.npmmirror.com

# 2. golangci-lint
curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin

# 3. Air
go install github.com/air-verse/air@latest

# 4. Swag
go install github.com/swaggo/swag/cmd/swag@latest

# 5. buf
BUF_VERSION="1.28.1"
curl -sSL "https://github.com/bufbuild/buf/releases/download/v${BUF_VERSION}/buf-Linux-x86_64" -o /tmp/buf
sudo install -m 755 /tmp/buf /usr/local/bin/buf
rm /tmp/buf

# 验证安装
echo "=== 验证 ==="
pnpm --version
golangci-lint --version
air -v
swag --version
buf --version
```

---

## 可选（非必需，可后续安装）

| 工具 | 说明 | 安装命令 |
|:---|:---|:---|
| Docker 镜像加速 | 国内拉取镜像更快 | 见 11-工具安装指南.md 第五章 |
| Jaeger | 链路追踪 | `docker run -d --name jaeger -p 16686:16686 jaegertracing/all-in-one:latest` |
| Prometheus | 指标监控 | `docker run -d --name prometheus -p 9090:9090 prom/prometheus:latest` |
| Grafana | 监控面板 | `docker run -d --name grafana -p 3001:3000 grafana/grafana:latest` |
| VS Code 扩展 | Go/Vue 开发扩展 | 在 VS Code 中手动安装 |