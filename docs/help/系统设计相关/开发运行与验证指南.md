# 开发、验证与贡献指南

本指南收拢本地开发、常用验证和贡献脚本的使用方式。命令默认在仓库根目录执行。

## 1. 第一次运行

```bash
cp .env.example .env
make docker-up
make migrate-up
STOP_EXISTING=true make dev-all
```

`make docker-up` 启动 PostgreSQL、Redis、NATS 等开发期依赖；`make migrate-up` 将数据库迁移到当前版本；`make dev-all` 启动 API、用户端、管理端和独立官方文档站。

常用地址：

| 服务 | 地址 |
| --- | --- |
| 用户端 | `http://localhost:3000` |
| 管理端 | `http://localhost:3001` |
| 官方文档 | `http://localhost:3002` |
| API | `http://localhost:8080/api/v1` |
| pgAdmin | `http://localhost:5050`（或 `.env` 中的 `PGADMIN_PORT`） |

环境变量的含义见 [env 文件配置说明](关于env文件配置.md)，数据库排障见 [数据库管理指南](数据库管理指南.md)。

## 2. `STOP_EXISTING=true make dev-all`

开发机上遗留的 API、Vite、管理端或文档站进程可能占用默认端口。带上 `STOP_EXISTING=true` 后，启动脚本会先停止占用 CampusOS 开发端口的旧进程，再启动当前工作区的服务：

```bash
STOP_EXISTING=true make dev-all
```

不加该变量时，脚本会保留已有进程并提示端口冲突，适合你明确知道现有服务应继续运行的场景。更多脚本行为见 [一键启动前后端脚本说明](一键启动前后端脚本说明.md)。

## 3. 日常验证

后端改动：

```bash
GOCACHE=/tmp/campusos-go-cache go test ./... -count=1
```

用户端改动：

```bash
cd web && pnpm lint && pnpm format:check && pnpm build
```

管理端改动：

```bash
cd admin && pnpm build
```

官方文档改动：

```bash
cd docs-site && pnpm build
```

数据库迁移改动：

```bash
make migrate-up
make migrate-status
```

文档或整体改动完成前：

```bash
git diff --check
make docs-links
```

`make docs-links` 检查当前 README、帮助/API/架构/Skill、v6 计划与进度、官方文档站、插件和 SDK 文档中的仓库内链接。最终验收统一执行：

```bash
RUN_RESTORE_DRILL=true RUN_BROWSER_SMOKE=true make release-check
```

该门禁包含 contracts、文档链接、全量 Go、数据库 migration/schema/架构、三类插件模板、Web lint/format/build、Admin/docs-site build、TypeScript SDK、恢复演练和真实 Chrome 工作流。

v0.5 主要 API 冒烟路径见 [v0.5 回归 Smoke 测试说明](v0.5回归Smoke测试说明.md)。

## 4. 提交和 PR 脚本

提交前请先确认 `git status --short`，不要把本地密钥、构建产物或其他开发者未关联的改动混入提交。

创建提交：

```bash
./sh/git_commit.sh "feat: describe the change"
```

脚本会检查当前改动、要求提交信息，并按脚本内配置执行基础检查。它不会代替你对改动范围和测试结果的判断。

创建 Pull Request：

```bash
./sh/git_pr.sh -t "feat: describe the change"
```

`git_pr.sh` 使用 GitHub CLI 创建 PR；运行前需要安装并登录 `gh`，并确认当前分支已推送。可用选项和 PR 自测清单见 [PR 提交脚本使用说明](../github使用相关/PR提交脚本使用说明.md) 与 [PR 模板与 CI 自测说明](../github使用相关/PR模板与CI自测说明.md)。

## 5. 工作原则

1. 插件、样式包和用户文件分别放在其职责目录中；不要把风格包源码放回 `data/plugins/`。
2. 不要用 scope 推断生效方式；按 `lifecycle.backend.activation_mode` 判断整站重启、插件重启或热更新，所有插件前端均热加载。详见 [插件分级与生命周期说明](../插件相关/插件分级与生命周期说明.md)。
3. 每个可验收任务都应新增对应的 `docs/进度/` 记录，并列出实际运行过的验证命令。
4. README 只保留当前基线和入口；操作细节应写入 `docs/help`、`docs/api` 或架构文档。
