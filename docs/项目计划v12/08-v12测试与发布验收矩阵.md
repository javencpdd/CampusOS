# CampusOS v12 测试与发布验收矩阵

> 状态：执行中；用于 Final 前的证据跟踪，不代表 v12 已发布  
> 最近更新：2026-07-19
> 关联：[v12 计划书](00-v12版本计划书.md)、[C3 进度](../进度/v0.12-dev/v0.12.15-dev.md)、[C3.5 健壮性加固](../进度/v0.12-dev/v0.12.17-dev.md)、[A7.1 验证码策略](../进度/v0.12-dev/v0.12.18-dev.md)、[A7.2 管理员准入](../进度/v0.12-dev/v0.12.24-dev.md)、[可靠 Worker 收敛](../进度/v0.12-dev/v0.12.19-dev.md)、[C3.6 分组与图文](../进度/v0.12-dev/v0.12.20-dev.md)、[Challenge PostgreSQL 兼容修复](../进度/v0.12-dev/v0.12.21-dev.md)、[Final 静态审计](../进度/v0.12-dev/v0.12.16-dev.md)

## 1. 使用规则

每一行都需要真实命令输出或可复现实验记录。代码、单元测试和文档齐全只能说明“实现完成”，不能替代
隔离数据库、恢复或浏览器证据。任何一项 P0 未验收时，应用版本保持 `0.11.0`，README 不得写
“v12 已完成”。

## 2. 验收矩阵

| 范围 | 关键证据 | 当前状态 | Final 结论 |
| --- | --- | --- | --- |
| Go 与模块边界 | `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1`；contracts、architecture、reliability、故障注入检查 | 静态门禁已通过；C3.5 增加编辑回滚、回收状态和错误脱敏测试 | 需在最终候选工作树重跑 |
| API/生成合同 | `make contracts-check`；OpenAPI、路由、授权矩阵 | 静态门禁已通过 | 需随 Final 版本变更重跑 |
| 互助/二手结构化写入 | `make mutual-aid-check secondhand-check` | 内存事务、状态机、Feature Gate、回收内容写保护和错误边界已覆盖 | 仍需真实 PostgreSQL |
| 分组导航与结构化图文 | contentbody/User Storage/Feature Catalog/Route Registry Go 测试；Web/Admin 组件测试与三端构建；`grouped-navigation-browser.mjs` | 点击 group 后显示子 board；互助/二手图片上传插入、服务端预览、安全清洗、纯文本兼容、独立 Feature 状态和路由目录同步已覆盖；PC/手机分组点击通过 | 分组导航已验收；真实图文上传、发布和编辑留 Final |
| 数据库 schema 与审计 | `./scripts/database-check.sh all`；架构同步检查 | schema/架构静态同步已通过 | 需连接隔离 PostgreSQL 执行 |
| v12 migration drill | `make v12-migration-check` | 已通过；`000028-000038` 与 v10/v11 兼容迁移均完成隔离 up/down/up | A7.2 已验收；Final 候选仍需重跑并留存完整日志 |
| 管理平面准入 | Identity/Middleware 单元测试；`make identity-admin-account-check`；Admin build；路由合同 | 普通用户不能使用 Admin 登录；非 scoped 管理 API 要求 Session + 活动准入 + Permission；版主 category scope 保持兼容 | 已验收；浏览器登录和停用准入留 Final |
| 可靠 Worker 收敛 | Reliability 单元/race、Admin 组件测试、`test-v12-reliability-worker-convergence-migration.sh` | Consumer Receipt 崩溃窗口、Complete/Retry/Dead 错误、8 次上限、103/8 历史事件、finalize 证据和脱敏 DTO 已覆盖 | 已验收；统一指标留待后续指标层 |
| 注册 Challenge PostgreSQL 兼容 | 锁键单元测试、`CAMPUSOS_IDENTITY_TEST_DATABASE_URL=... go test ./internal/modules/core/identity/repository -run TestPgChallengeRepositoryTryConsumeRate`、真实 HTTP 请求与 Outbox 核对 | advisory lock 键不含 NUL；请求返回 200，Challenge/限流/审计同事务提交，事件一次投递为 published | 已验收；Final 仍需 SMTP Provider 浏览器流程 |
| C3 二手迁移 | `./scripts/test-v12-secondhand-migration.sh` | 已通过；覆盖类型、作者、价格、币种、触发器、约束和 re-up | 已验收 |
| 恢复演练 | `make restore-drill` | 未执行 | 阻塞；需核对恢复后的 migration、约束和数量 |
| 冷启动 | `STOP_EXISTING=true make dev-all` | A0.5 有历史基线证据；Final 候选尚未重跑 | 需专用测试栈执行 |
| Web/Admin/docs-site | lint、component test、build、文档链接 | 静态构建已通过 | 需在 Final 候选重跑 |
| 浏览器流程 | 注册、登录、会话、板块、RichText、互助、二手的 PC/移动端 smoke | 未执行 | 阻塞；仅允许在可清理的测试库运行 |
| 完整发布门禁 | `make release-check` | 未执行；已修正其漏跑 v12 migration drill 的问题 | 最后执行，保存完整日志 |

## 3. 推荐执行顺序

1. 准备隔离 PostgreSQL 与专用测试数据库，确认不会写入日常开发或生产数据。
2. 执行 `make v12-migration-check`。C3 的脚本失败时先修 migration，不进入恢复或浏览器阶段。
3. 执行 `make database-check`、`make restore-drill`，记录 schema、约束、数据数量和恢复结果。
4. 从候选工作树执行 `STOP_EXISTING=true make dev-all`，保存四个服务就绪证据。
5. 在该测试栈运行浏览器 smoke，以及 group 点击子菜单、互助/二手图片上传预览、桌面/窄屏写入与停用
   Feature 流程。
6. 停止测试栈后运行 `make release-check`。只有该步骤完整通过才更新版本、SDK、OpenAPI 和 README。

## 4. C3 最小迁移验收

`000035_v12_secondhand` 必须证明：

- Detail 只能关联 `thread_type=secondhand` 的基础 Thread，且 `created_by` 与作者一致。
- 负价格、非 CNY、非法枚举、无效版本和超长地点被约束拒绝。
- down 后表、触发器和约束消失；再次 up 后表、触发器和关键约束恢复。
- 正常数据的创建、读取和索引存在性可验证。

`000036_v12_identity_challenge_policy` 必须证明：

- 默认策略为每邮箱 10 分钟 5 次、每 IP 60 分钟 10 次。
- 次数为零或窗口越界被数据库约束拒绝。
- down 后旧 Scope 约束恢复，re-up 后策略表和约束重新建立。
- 新权限只默认授予 admin，策略更新有版本冲突和 required audit。

`000037_v12_outbox_worker_convergence` 必须证明：

- `system:outbox-finalize` 可记录 `failed`，down 前安全映射为旧约束允许的 `retry`。
- attempts 等于 max 的 pending/retry/过期 processing 不再领取并进入 dead；历史 `103/8` 保留 103。
- `7/8` 的过期 processing 可由新 Worker 领取为 `8/8`，旧 generation Complete 被 fencing 拒绝。
- PostgreSQL Complete、Retry 和 DeadLetter 使用相同 owner/generation 条件并能实际提交。

`000038_v12_admin_accounts` 必须证明：

- 升级前每个全局 admin 都有且仅有一个活动邮箱凭据，否则迁移在建表前失败。
- 管理员密码不复制，准入表只引用 Identity 用户和凭据；非法状态和不一致外键被数据库拒绝。
- admin 角色分配、撤销和重新分配会同步 `active/revoked`，独立 `suspended` 不被普通角色刷新覆盖。
- down 只删除准入表和同步触发器，不删除用户、账号或角色；re-up 能从现有全局角色重新回填。

## 5. 当前阻塞与解除条件

隔离迁移演练已经解除阻塞。当前剩余阻塞是全量数据库审计、备份恢复、最终候选冷启动、真实浏览器流程和
完整 `make release-check`；不得用本次迁移日志替代这些独立证据。
