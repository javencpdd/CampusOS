# CampusOS v1.1.31-dev 进度说明

> 日期：2026-09-24  
> 更新时间：2026-09-24 23:50（Asia/Shanghai）  
> 状态：已完成文档同步；不构成 v1.1 Final 发布声明。  
> 关联工作：按当前代码重构状态更新 3002 官方文档站、v1.1 计划文档和仓库 README。

## 1. 本次目标

消除官方说明中将旧 `data/plugins`、Plugin UI v2、`builtin.pdf-viewer` 当作当前开发入口的表述，
让 3002 文档站能以真实的 `campusos.pdf-viewer` v4 外部插件帮助新开发者理解目录、Manifest、隔离 UI、Bridge、三层授权、用户配置和 Docker 验证。

## 2. 完成内容

1. 重写插件导航、首页、体系概览、Manifest、三层授权、打包安装、可信市场、CLI、兼容性及隔离 UI 文档；旧 v1-v3 教程保留为明确的历史兼容页，不再作为新插件模板。
2. 新增 `docs-site/plugins/pdf-viewer-tutorial.md`，以 `plugins/campusos.pdf-viewer` 的真实 v4 目录、`runtime: none`、PDF 三类资源、Bridge Range、用户配置目录、打包安装和 Docker `rebuild` 为端到端入门示例。
3. 更新入门、架构、模块边界、数据目录、开发配置、Docker 部署和当前路线，使其描述 `plugins/.installed`、3003 Plugin UI Gateway、用户配置目录及现存限制。
4. 精简根 `README.md` 为启动、端口、源码加载规则、目录边界和权威文档入口；同步仓库 `docs/README.md`。
5. 在 v1.1 原计划、旧审查、重构计划和 ADR 中增加 2026-09-23 状态追记，清楚区分已实现代码、待验收证据和历史方案；不修改原始“思路”输入材料。
6. 修正 v4 CLI 文档中的签名参数：签名输入为 release archive，并通过 `--out` 生成待安装的 signed archive；避免新开发者按教程安装未签名包。

## 3. 当前结论

- 应用运行时版本字符串仍为 `v0.13.0`；当前重构属于 `v1.1-dev`，不能据文档更新宣称 v1.1 Final。
- `campusos.pdf-viewer` 是唯一当前 PDF 预览外部插件；旧 `builtin.pdf-viewer` 与 Plugin UI v2 只保留历史说明。
- 通用 Wasm/container Runtime、完整安装状态机、升级回滚、第二示例/独立 CI，以及目标 Windows/Linux 浏览器、LAN、恢复和性能证据仍是后续/验收工作。

## 4. 验证结果

| 检查 | 结果 |
| --- | --- |
| `python scripts/check-doc-links.py` | 通过：927 条本地链接有效。 |
| README 结构与链接检查 | 通过：根 README 89 行、所需“当前状态/快速开始/文档”章节齐全，`README.md` 与 `docs/README.md` 链接有效。 |
| `docker compose -f compose.dev.yml exec -T docs pnpm build` | 已通过最近一次成功构建：VitePress 完成 client/server bundle 与页面渲染，并修复旧 `ui-v2.md` 和插件源码相对链接造成的死链；随后仅调整 CLI 代码示例文本。 |
| `git diff --check` | 通过。 |
| `python scripts/check-line-endings.py --include-untracked` | 通过：0 个换行策略错误。 |
| `make readme-check` / `make docs-links` | 当前 Windows PowerShell 环境未安装 `make`，改用上述仓库内 Python 检查器完成等价 README/链接验证。最后一次 Docker 构建重跑因审批额度限制未执行，未绕过该限制。 |

本次工作不执行 Git commit 或 push。
