# CampusOS v1.1.30-dev 进度说明

> 日期：2026-09-23  
> 更新时间：2026-09-23 02:02（Asia/Shanghai）  
> 状态：已完成  
> 关联工作：将导入的高级开发工程师专家包适配为 CampusOS 仓库级、可发现的工程工作法 Skill。

## 1. 本次目标

把 `skills/sources/senior-developer/` 中的通用专家资料转化为可直接服务 CampusOS 开发的工作流：保留来源可追溯性，
同时让“小步实现与验证”与现有版本进度、模块边界、权限、迁移、Docker 和专项 Skill 协同。

## 2. 完成内容

1. 新增根级 `skills/sources/senior-developer/SKILL.md`，成为唯一规范且可自动发现的 `senior-developer` 入口。
   其中固化“建立最小范围 → 读/写/验/记录 → 立即验证 → 如实交付”的 CampusOS 工作法。
2. 规范明确了 Core/Feature、Web/Admin、附件存储、迁移、外部插件与 Docker 的边界，以及与
   `campusos-project-onboarding`、`campusos-dev-nocommit`、`campusos-dev-workflow` 和各专项 Skill 的组合方式。
3. 原始角色正式更名为“学长小文”；`agents/`、嵌套通用 Skill、字体、模板与脚本继续作为导入参考，不作为自动调用、
   安装外部 CLI 或访问外部服务的授权。
4. 已生成 `.agents/skills/senior-developer/SKILL.md` 仓库发现桥接。clone 后可从仓库内调用，不需要安装到用户级目录。
5. 重写 `skills/guides/高级开发工程师专家包使用与接入说明.md`，同步 Skills 索引和官方文档贡献流程，
   说明何时调用、验证基线、安全边界、维护命令及原始参考资料的正确用法。

## 3. 关键文件

| 范围 | 文件 |
| --- | --- |
| 规范源与角色 | `skills/sources/senior-developer/SKILL.md`、`README.md`、`agents/senior-developer.md`、`MANIFEST.md`、`MIGRATION.md` |
| 自动发现 | `.agents/skills/senior-developer/SKILL.md`（由同步脚本生成） |
| 使用说明 | `skills/guides/高级开发工程师专家包使用与接入说明.md`、`skills/guides/README.md` |
| 官方文档 | `docs-site/contributing/workflow.md` |

## 4. 验证结果

| 检查 | 结果 |
| --- | --- |
| `sync_skill_bridges.py --root . --write` | 通过，识别并同步 9 个规范源 Skill。 |
| `sync_skill_bridges.py --root . --check` | 通过，9 个规范源 Skill 无桥接漂移。 |
| `quick_validate.py`（全部 `skills/sources/*` 与 `.agents/skills/*`） | 通过，9 个规范源和 9 个桥接均有效。 |
| `python scripts/check-doc-links.py` | 通过，953 条本地链接有效。 |
| `git diff --check` | 通过。 |
| `python scripts/check-line-endings.py --include-untracked` | 通过，0 个换行策略错误。 |
| `docker compose -f compose.dev.yml exec -T docs pnpm build` | 未通过：`docs-site/plugins/ui-v2.md` 中两条既有死链（指向 v1.1 计划与插件架构文档）使 VitePress 中止；本次未改动该文件。 |

## 5. 使用提示

开发任务可以直接组合调用：

```text
使用 $senior-developer 与 $campusos-dev-nocommit 完成此项 CampusOS 修复；
按最小纵向切片实现并立即验证，不提交或推送。
```

若当前已打开的 Codex 会话未显示 `$senior-developer`，重启 Codex 一次以刷新仓库级 Skill 列表。

本次变更保持未提交，未执行 Git commit 或 push。
