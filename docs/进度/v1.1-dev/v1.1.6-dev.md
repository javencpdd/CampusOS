# CampusOS v1.1.6-dev 架构文档更新时间元数据补齐

> 状态：已完成
>
> 更新时间：2026-09-12（Asia/Shanghai）
>
> Git：本记录对应的修改保持未提交、未推送。

## 1. 本次目标

修正当前架构文档在完成 v1.1.5 附件存储、PDF Plugin UI v2 双入口和个人空间权限模型更新后，未在文首显示
明确更新时间的问题。

## 2. 完成内容

- `docs/architecture/当前架构概览.md` 增加“更新时间”和本次更新范围，读者可直接确认附件存储结构、预览接入与
  owner-only 权限模型对应的文档更新时间。
- `docs/architecture/README.md` 增加“更新时间”与历史边界说明。
- 未给 `v0.7`–`v0.13` 历史架构快照补写当前日期：这些文件的冻结日期或最后核验日期是历史事实，重写为当前日期会
  混淆其适用版本和设计时间。

## 3. 验证结果

```text
python scripts/check-doc-links.py                         # 通过
git diff --check                                          # 通过
python scripts/check-line-endings.py --include-untracked  # 通过
```

## 4. 后续

后续变更当前架构、目录边界、迁移投影或权限模型时，必须同步更新“当前架构概览”文首的更新时间和更新范围；历史
版本文档仅在纠正其历史事实时更新，并保留其版本/核验时间语义。
