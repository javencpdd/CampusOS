# CampusOS v1.1.16-dev 迁移链整体重构记录

> 日期：2026-09-16
> 更新时间：2026-09-16（Asia/Shanghai）
> 状态：已完成；未提交、未推送。

## 1. 本次目标

在项目所有者确认“当前开发数据均可清空”的前提下，移除 v1.1 阶段分散的 `000001`–`000011` migration 链，建立可从零创建当前代码所需完整数据模型的单一 v1.1 baseline，并同步重置 Docker 开发测试库及全部架构投影。

## 2. 完成内容

1. 新增 `000001_v1_1_schema_baseline.up/down.sql`：整合原 Schema、插件三层授权和参考数据、Runtime 修正、图文附件、资产治理、个人 Asset 与 Personal Documents PDF Invocation 上下文；旧 `000002`–`000011` 文件已移除。
2. 修复合并后 SQL 的 `search_path`：原 dump 完成后显式恢复 `public, pg_catalog`，使保留原始无前缀写法的授权/参考数据/扩展 DDL 在同一原子 migration 中正确落到 `public`。
3. 更新 baseline drill、迁移静态检查、Admin `/architecture` 的迁移时间线与全部表来源标记；ER PNG、SVG 和中文关系说明重新生成，结果为 88 张业务表、105 条物理外键及 90 张 public 表（含两张执行器系统表）。
4. 更新 migration、架构、Help、docs-site、v1.1 计划书及历史 v1.0 说明的状态边界：旧链是历史记录，当前行为以单一 v1.1 baseline 为准；后续 Schema 从 `000002` 追加。
5. 已精确确认并 reset Docker `campusos-dev-postgres-1` 中的 `campusos` 开发测试库：旧链 11 条记录和数据已删除，新的 `000001` 已应用；API 已重新启动并完成受控 bootstrap、内置插件注册。

## 3. 修改文件

- `migrations/000001_v1_1_schema_baseline.*.sql`、已删除的 `migrations/000002_*` 至 `000011_*`：单一 baseline。
- `scripts/test-v1-database-baseline.sh`、`scripts/check-*.sh`、`skills/sources/campusos-data-architecture-sync/scripts/check_architecture_sync.py`：新的单基线回归与投影检查。
- `migrations/README.md`、`migrations/er/current/`、`admin/src/modules/architecture/pages/SystemArchitectureView.vue`：Schema、ER 与管理端投影。
- `docs/architecture/`、`docs/help/系统设计相关/`、`docs-site/`、`docs/项目计划书v1/项目计划v1.1/00-v1.1版本计划书.md`：当前操作和设计说明。

## 4. 验证结果

- Git Bash：`POSTGRES_CONTAINER=campusos-dev-postgres-1 DB_USER=campusos DB_PASSWORD=campusos_dev ./scripts/test-v1-database-baseline.sh` 通过；隔离库验证 reset、checksum drift、单基线 down/up 和 90 张 public 表。
- Docker 开发库：`./scripts/migrate.sh reset` 后 `status` 显示仅 `000001_v1_1_schema_baseline`；数据库查询确认 `migrations=1`、`public tables=90`。
- `./scripts/database-check.sh all`：审计 0 违规、Schema Contract pass、migration hygiene 0 问题。
- `python migrations/tools/generate_er.py --check`、`python skills/sources/campusos-data-architecture-sync/scripts/check_architecture_sync.py --root .`、`python scripts/check-doc-links.py`：通过。
- Git Bash 静态 migration 检查及 PowerShell `./scripts/migrate.ps1 check`：通过。
- Docker Admin：`pnpm build` 通过；API `/api/v1/health` 返回 `status=ok`，启动日志确认已加载 3 个插件。

## 5. 后续任务

1. 当前 `000001` 是已重建开发/测试基线；共享或不可丢弃环境不得修改它，新的结构演进从 `000002` 成对 migration 开始。
2. 正式环境如有真实历史数据，必须另行批准导出、转换、备份恢复和前向迁移方案；不得使用本次 reset 流程。
3. v1.1 Final 仍需目标 Linux 的隔离、容量、恢复和浏览器证据；本次 Schema 重构不替代这些发布门禁。
