# CampusOS v1.1.18-dev 架构文档同步记录

> 日期：2026-09-16  
> 更新时间：2026-09-16（Asia/Shanghai）  
> 状态：已完成；未提交、未推送。

## 1. 本次目标

核对 `docs/architecture` 是否仍以 v1.1 为当前事实，并将 v1.1.17-dev 修复的插件授权版本 ID 精度边界与个人空间孤儿文件清理条件同步进架构文档。

## 2. 完成内容

1. 确认架构入口已声明当前范围为 `v1.1-dev`；旧 `v0.7`–`v0.13` 文件由“历史文档说明”标注为追溯资料，不是当前实现说明。
2. 在“插件平台与授权体系”补充 Snowflake `BIGINT` 必须作为十进制字符串通过 Browser/API 边界的规则，以及把该值转为 JavaScript number 会错误定位插件版本的原因。
3. 在“数据库与部署运维”补充 `data/personal-space` 的清理前提：只能在测试环境、数据库对象/资产/文档引用均为零时清理历史孤儿文件，并保留 `.gitkeep`。
4. 更新总览日期及其插件授权 ID 边界摘要。

## 3. 修改文件

- `docs/architecture/当前架构概览.md`
- `docs/architecture/模块设计/插件平台与授权体系.md`
- `docs/architecture/模块设计/数据库与部署运维.md`

## 4. 验证结果

- 只读 Docker PostgreSQL 查询：当前仅有初始化管理员；`storage_objects`、`user_assets`、`personal_documents` 均为 0，因此现存个人空间文件属于无引用测试遗留。
- `python scripts/check-doc-links.py`：通过，833 条本地链接有效。
- `python scripts/check-line-endings.py --include-untracked`：通过。
- `git diff --check`：通过。

## 5. 后续任务

1. 新增模块、存储路径、授权能力或迁移基线时，继续同步当前架构专题；历史 ADR/快照仅保留决策追溯。
2. 清理测试孤儿文件前保持 `.gitkeep`，且不以目录是否存在替代数据库引用检查。
3. 本次没有提交或推送。
