# CampusOS v1.1.33-dev 进度说明

> 日期：2026-09-25  
> 更新时间：2026-09-25 00:31（Asia/Shanghai）  
> 状态：已完成  
> 关联工作：修复路由合同漂移及插件中心组件测试失败。

## 1. 问题与原因

- 代码已注册 `POST /api/v1/richtext/articles/:id/attachments/from-personal-document`，但路由 JSON、授权矩阵和 OpenAPI 生成文件尚未同步，因此合同检查报漂移。
- 插件中心加载流程新增可信插件市场来源请求；组件测试未模拟该请求，导致并行加载提前失败，插件目录未展示。测试还保留了旧版申请入口文案。

## 2. 完成内容

- 使用项目合同生成器同步路由 JSON、授权矩阵和 OpenAPI；仅补齐上述已存在路由，未更改路由或鉴权实现。
- 为插件中心组件测试补充可信市场来源模拟和调用断言，并将入口文案断言调整为当前页面的“从可信插件市场申请”。
- 未修改插件运行逻辑、数据库或用户数据。

## 3. 修改文件

- `docs/api/http-routes-v0.6.json`
- `docs/api/HTTP路由与授权矩阵-v0.6.md`
- `docs/api/openapi-v0.6-current.yaml`
- `web/tests/plugin-center.spec.ts`

## 4. 验证结果

| 检查 | 结果 |
| --- | --- |
| `go run ./cmd/campusos-contracts --check` | 通过，10 项合同文件均一致。 |
| `go run ./cmd/campusos-plugin-v4-check -root plugins` | 通过。 |
| `web` 的 Vitest 全集 | 通过，14 个测试文件、30 项测试。 |
| `prettier --check tests/plugin-center.spec.ts` | 通过。 |
| `python scripts/check-doc-links.py` | 通过，927 个本地链接有效。 |
| `git diff --check` | 通过。 |
| `python scripts/check-line-endings.py --include-untracked` | 通过，0 个错误。 |

本次修改未执行 Git commit 或 push。
