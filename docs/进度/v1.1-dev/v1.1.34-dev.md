# CampusOS v1.1.34-dev 进度说明

> 日期：2026-09-25  
> 更新时间：2026-09-25 00:45（Asia/Shanghai）  
> 状态：已完成  
> 关联工作：将导入的 software-company 专家包适配为仓库级 Skill。

## 1. 本次目标

把原 WorkBuddy 多角色专家包中的产品、架构、工程、测试和交付协作方法融入 CampusOS 开发，同时保持原始资料可追溯、Skill 可从仓库直接发现，并提供使用说明。此次不修改系统运行代码。

## 2. 完成内容

- 新增 `skills/sources/software-company/SKILL.md` 和按需读取的角色交接清单，按任务复杂度选择协作视角；小型修复不强制团队流程。
- 将原包的 `TeamCreate`/`SendMessage`、默认 React/Python 栈、批量写完再统一验证、固定两轮测试等约定明确限定为历史来源，不作为 CampusOS 指令；以现有 Go、Vue、PostgreSQL、Docker、插件和权限边界为准。
- 保留导入的五个角色文件和元信息供溯源，并在原导出清单、迁移指南顶部标明其历史快照性质，避免当前使用者误用旧说明。
- 生成 `.agents/skills/software-company/` 仓库发现桥接，新增中文使用指南，同步 Skills 索引和官方文档贡献流程。
- 未将 Skill 安装到用户级目录；未创建子代理、修改用户数据或执行 Git 提交/推送。

## 3. 修改文件

- `skills/sources/software-company/SKILL.md`
- `skills/sources/software-company/references/campusos-roles.md`
- `skills/sources/software-company/MANIFEST.md`、`MIGRATION.md`（导入快照提示）
- `.agents/skills/software-company/SKILL.md`
- `skills/guides/软件团队协作Skill使用说明.md`、`skills/guides/README.md`
- `docs-site/contributing/workflow.md`

`skills/sources/software-company/agents/`、`.codebuddy-plugin/` 和 `.downloaded_at` 为用户提供的未跟踪原始导入资料，本任务保留原样，以便随本 Skill 一起纳入仓库。

## 4. 验证结果

| 检查 | 结果 |
| --- | --- |
| `python skills/sources/campusos-skill-repository-sync/scripts/sync_skill_bridges.py --root . --check` | 通过，10 个规范源无桥接漂移。 |
| `quick_validate.py` 对 `skills/sources/` 和 `.agents/skills/` 全部目录 | 20/20 通过。 |
| `python scripts/check-doc-links.py` | 通过，938 个本地链接有效。 |
| `docs-site` VitePress build | 通过。 |
| `git diff --check` | 通过。 |
| `python scripts/check-line-endings.py --include-untracked` | 通过，0 个错误。 |

## 5. 后续任务

仓库克隆后可从 `.agents/skills/software-company/` 使用；已经打开的 Codex 会话若未刷新 Skill 列表，可重启一次。此 Skill 是开发工作法，不是 CampusOS 运行时插件。未执行 Git commit 或 push。
