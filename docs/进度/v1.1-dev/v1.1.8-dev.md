# CampusOS v1.1.8-dev 架构历史文档适用性清理

> 更新时间：2026-09-14（Asia/Shanghai）
> 状态：已完成；未提交、未推送。

## 1. 本次目标

消除旧架构资料被当成当前实施依据的误导，保留可追溯的决策与引用。

## 2. 完成内容

使用 campusos-readme-update 的历史路径保留规则，修改而不删除既有文档。
九份版本专题逐份标注具体失效范围与替代专题；十二份 ADR 增加适用性声明。
旧路径、章节、原始日期和 JSON 快照保持不变，不伪造历史内容已在本日复验。
当前入口强调旧迁移编号、源码路径和历史完成状态不可直接使用。

## 3. 修改文件

- docs/architecture 下九份 v0.7–v0.13 Markdown（完整清单见 [历史索引](../../architecture/历史文档说明.md)）。
- docs/architecture/decisions 下 ADR-0011 至 ADR-0022，共十二份文件。
- docs/architecture/README.md、历史文档说明.md。
- 本进度文档。

扫描确认两份 JSON 快照由历史基线/进度引用；未发现 internal、scripts、Makefile 对它们的直接引用。
本次未删除文件、改动运行逻辑或迁移数据；工作区其他既有修改不属于本任务。

## 4. 验证结果

- python scripts/check-doc-links.py：通过，767 条本地链接。
- python skills/sources/campusos-readme-update/scripts/audit_readme_structure.py --root .：通过；根 README 的既存篇幅及链接数量提醒仍保留。
- python skills/sources/campusos-readme-update/scripts/check_readme_links.py --root . README.md docs/README.md docs/architecture/README.md docs/进度/v1.1-dev/v1.1.8-dev.md：通过。
- python scripts/check-line-endings.py --include-untracked：通过，错误数为零。
- git diff --check：通过。

本次仅修改说明，不运行构建、迁移或容器。

## 5. 后续任务

新开发只从当前架构概览与模块专题进入。未来重大决策变更应明确替代具体 ADR，
不可仅以文档年代推断安全约束失效。
