# CampusOS v1.1.9-dev 架构与源码事实同步

> 更新时间：2026-09-14（Asia/Shanghai）
> 状态：文档修订完成，验证见下文；未提交、未推送。

## 1. 本次目标

以实际工作区入口、装配、模块、路由和配置实现修订架构，撤下已失效正文，不再仅添加历史警告。
依据当前 v1.1-dev 进度及正式计划处理本任务，不采用 Skill 中已滞后的阶段快照。

## 2. 源码核验与修正

- 阅读 cmd/server、Server.Run、bootstrap、application、platform_modules、部署校验、Runtime/Registry 生命周期。
- 核对七个核心目录、十三个内置业务目录的 ModuleID/Dependencies 与嵌入 YAML；区分启动图和功能目录图。
- 核对 Go 与三份前端包声明、双前端 main/Router/Vite 代理、HTTP Feature Gate、Identity health。
- 核对 Config.Load、直接环境读取的插件秘密、Storage Module、附件 owner/Invocation 流程与开发 watcher。
- 新增精确的启动链、配置优先级、单实例限制、PG/NATS 内存回退、静态健康响应及 watcher 失败保留旧进程说明。
- 记录 YAML/Go 依赖不一致和 integration.* 运行 ID，不把目录声明误认为精确启动依赖。
- 九份旧专题撤下正文、保留短导航；未移动或删除仍有引用的文件。

本次是架构入口与相关实现的静态核验，不宣称逐行审计了全部业务源码，也不宣称运行环境已通过发布验收。

## 3. 文件与引用调整

新增：

- [源码目录与启动装配](../../architecture/源码目录与启动装配.md)
- [配置与运行边界](../../architecture/配置与运行边界.md)

修改：当前架构概览、README、历史文档说明，插件平台、数据库部署、用户存储、身份、可靠事件专题；
九份旧文档的逐项替代关系见 [替代清单](../../architecture/历史文档说明.md)。

删除的是九份文档中失效的正文，不是文件路径；历史原文仍可在 Git 中恢复。
修改前扫描 docs、docs-site、skills、README、internal、scripts 的文件名引用，并搜索 Markdown 锚点引用。
旧路径仍有引用，未发现旧专题的锚点消费者，因此保留文件级兼容入口，无需改写历史进度。
JSON 快照与 ADR 是有引用的历史证据，未删除或移动，也不纳入当前实现权威。

工作区此前已有的业务、迁移、ER 和其他文件改动全部保留，本次不修改代码或数据。

## 4. 验证结果

- python scripts/check-architecture-boundaries.py：通过，0 legacy sources、0 legacy concrete edges。
- python scripts/check-frontend-boundaries.py：通过。
- python scripts/test-architecture-checks.py：9 项测试通过。
- 新增专题的 check_readme_links.py 检查：通过。
- python scripts/check-doc-links.py：通过，818 条本地链接。
- audit_readme_structure.py --root . 并为两份新增专题指定 --require-doc：通过；保留根 README 既存篇幅和直接链接数量提醒。
- check_readme_links.py --root . README.md docs/README.md docs/进度/v1.1-dev/v1.1.9-dev.md：通过。
- python scripts/check-line-endings.py --include-untracked：通过，0 错误。
- git diff --check：通过。

纯文档变更，不运行容器、数据库迁移、Go 全量构建或浏览器验收。

## 5. 后续边界

本次如实记录依赖声明差异、内存回退与预览插件交付形态，没有将其改写为已修复。
后续代码变更应同步当前专题及生成合同；架构一致性不等于 v1.1 Final 发布完成。
