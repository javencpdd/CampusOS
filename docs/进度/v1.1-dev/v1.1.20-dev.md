# CampusOS v1.1.20-dev 插件自包含重构计划编制

> 日期：2026-09-21
> 更新时间：2026-09-21（Asia/Shanghai）
> 状态：计划编制完成；新重构尚未实施，不是 v1.1 Final。

## 1. 本次目标

按照项目所有者要求，为插件全部功能、用户/管理员页面与配置定义集中到根 `plugins/`、
可独立安装卸载，以及用户添加时自动创建专属配置目录，编写新的 v1.1 实施计划。
本轮限定为设计与文档，不因允许重构删除而提前删除源码或数据。

## 2. 完成内容

1. 核对 `trustedModules.ts`、动态路由注册、PDF builtin Manifest、Package/Manager、用户 Grant、
   插件 SQLite/config 路径及现有通用数据库表，确认仅移动 Vue 文件不能实现真正独立安装。
2. 新计划覆盖包目录与 Manifest、隔离 UI/Bridge、后端发现及 Runtime、管理员配置、用户目录 provisioning、
   Grant/Consent、版本/配置升级、启停卸载、存储与索引、Docker、SDK/CLI、12 项 WBS、排期资源和验收。
3. 明确采用跨 Origin 插件页面，不在主站动态执行上传的 JS；浏览器能力依据官方文档核对并保留参考链接。
4. 用户目录由宿主在“添加”时创建，采用可靠操作/Outbox/幂等/原子文件替换；添加、配置就绪、启用、同意权限分开。
5. 标注旧 00 计划为背景、旧 01 审查仅针对旧范围；新 02 计划为本轮范围权威，不将过去测试当作新功能验收。
6. 同步文档门户、v1 索引和系统设计入门；当前架构仅增加“目标方案尚未实施”的提示，未伪造代码事实。

## 3. 修改文件

- 新建 `docs/项目计划书v1/项目计划v1.1/02-v1.1插件自包含与动态加载重构计划书.md`。
- 新建 `docs/help/系统设计相关/插件自包含动态加载与用户配置设计.md`。
- 更新旧 `00-v1.1版本计划书.md`、`01-v1.1代码完成情况审查与项目回顾.md` 的范围声明，保留正文和原链接。
- 更新 `docs/项目计划书v1/README.md`、`docs/README.md`、`docs/计划书总结/README.md` 的计划入口和状态。
- 更新 `docs/help/系统设计相关/插件存储边界与平台通用数据设计.md`、`v1.1个人资源预览与授权设计说明.md` 和 `docs/architecture/模块设计/插件平台与授权体系.md` 的新目标入口。
- 本文件为本次唯一新增进度记录；其他未提交代码及 `migrations/er/20260919/`、`migrations/tools/README.md` 均未处理。

## 4. 验证结果

- 只读架构同步检查：`python skills/sources/campusos-data-architecture-sync/scripts/check_architecture_sync.py --root .`，90 张表与 Admin 静态架构一致；本轮没有修改 migration。
- `python scripts/check-doc-links.py`：912 条本地链接通过。
- `python scripts/check-line-endings.py --include-untracked`、`git diff --check`：通过。
- `python skills/sources/campusos-readme-update/scripts/audit_readme_structure.py --root . --require-doc docs/项目计划书v1/项目计划v1.1/02-v1.1插件自包含与动态加载重构计划书.md --require-doc docs/help/系统设计相关/插件自包含动态加载与用户配置设计.md`：通过，新文档可从门户到达；根 README 原有行数/直接链接数量建议性告警保留。
- `python skills/sources/campusos-readme-update/scripts/check_readme_links.py --root . README.md docs/README.md`：通过。
- 未执行 Go/前端构建、数据库演练、Docker 修改或浏览器回归：本轮仅设计文档，不把方案当作代码交付。
- 参考了 MDN iframe/postMessage/CSP、Vue Router 动态路由、PDF.js Range Transport 及 Docker 官方安全说明；本地代码核对优先于旧计划中的实现断言。

## 5. 后续任务

按照新计划 R11-01 冻结合同和威胁模型，再先验证隔离 UI/PDF.js Worker/Range 原型；
原型通过后才实施包、数据库、Runtime、用户配置和迁出。新增合同/CLI/目录均不能提前作为当前使用说明。
所有新 WBS 仍为待实施，未承诺完成时间或发布状态。

没有删除源码、插件、用户文件或数据库；没有安装新依赖；未提交、未推送。
工作期间 HEAD 从 `505d3e8` 推进至 `cbb19b6`，本任务未执行该提交，也未回退该变化；本次新增改动均为文档。
