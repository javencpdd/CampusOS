# CampusOS v1.1.35-dev 进度说明

> 日期：2026-09-25
> 更新时间：2026-09-25 01:13（Asia/Shanghai）
> 状态：已完成
> 关联工作：应用运行版本与当前 v1.1-dev 开发阶段同步。

## 1. 本次目标

结束“当前源码处于 v1.1-dev，但启动日志和 API 仍显示 v0.13.0”的混用。采用 `v1.1.0-dev` 作为符合三段版本号的开发构建标识；它对应 `v1.1-dev` 进度目录，不表示 `v1.1 Final` 已验收或发布。

## 2. 完成内容

- 将应用版本源 `internal/platform/version/version.go` 改为 `1.1.0-dev`，同步 CLI、API 元信息、Go SDK `v1.1`、TypeScript SDK 与文档站包版本。
- 扩展版本同步检查与 README 结构审计，使其识别预发布后缀，并核对 README、文档门户和官方文档入口同时展示开发应用版本及开发阶段。
- 用生成器更新路由 JSON、授权矩阵与 OpenAPI 的内容版本：分别为 `v1.1` 和 `1.1.0-dev-experimental`。历史兼容文件名及 `/api/v1`、Manifest、Host API、Bridge 等独立协议标识不改名。
- 同步根 README、架构/API/Help 当前入口与官方文档站。正式发布基线和部署 Compose 的默认正式镜像标签仍为 `v0.13.0`；本次不把开发构建称为正式发布，也不修改用户数据。

## 3. 修改范围

- 版本与门禁：`internal/platform/version/version.go`、`scripts/check-version-sync.py`、`skills/sources/campusos-readme-update/scripts/audit_readme_structure.py`。
- 包元信息：`sdk/go/hostapi.go`、`sdk/typescript/package.json`、`docs-site/package.json`。
- 生成合同：`docs/api/http-routes-v0.6.json`、`docs/api/HTTP路由与授权矩阵-v0.6.md`、`docs/api/openapi-v0.6-current.yaml`。
- 当前入口：`README.md`、`docs/README.md`、`docs/architecture/`、`docs/api/API索引.md`、`docs/help/` 的当前导航/运行指南、`docs/计划书总结/` 的当前计划入口、`docs-site/` 的首页与当前使用说明。

## 4. 验证结果

| 检查 | 结果 |
| --- | --- |
| `go run ./cmd/campusosctl version` / `version --short` | 分别输出 `v1.1.0-dev` / `1.1.0-dev`。 |
| 已运行 Docker 开发 API 的 `GET /api/v1` | `application_version` 实际返回 `v1.1.0-dev`；API 路由合同 `version` 仍为独立的 `v1`。 |
| `python scripts/check-version-sync.py` | 通过：应用 `1.1.0-dev`、Go SDK `v1.1`、OpenAPI `1.1.0-dev-experimental`。 |
| `go run ./cmd/campusos-contracts --check` | 通过，所有生成合同一致。 |
| `go test ./... -count=1` | 通过。 |
| `docs-site` VitePress build、TypeScript SDK `tsc -p tsconfig.json` | 均通过。 |
| README 结构/链接检查、全仓本地文档链接检查 | 均通过。 |
| `git diff --check`、`python scripts/check-line-endings.py --include-untracked` | 均通过，换行检查 0 个错误。 |

## 5. 后续边界

- `v0.13.0` 只保留为当前正式发布/默认部署镜像基线，不再冒充当前源码的开发运行版本。正式发布版本与 Docker 镜像标签的推进需要独立发布验收。
- 本次未做 v1.1 的浏览器、恢复、性能或 Final 验收，也未执行 Git commit 或 push；现有工作区的其他未提交 Skill 改动保持原样。
