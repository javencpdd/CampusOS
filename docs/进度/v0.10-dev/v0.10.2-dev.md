# CampusOS v0.10.2-dev：A1 实测问题复现与责任映射

> 日期：2026-07-16  
> 实现状态：已完成  
> 验收状态：已在 A15 的统一门禁复验

## 阶段目标

把十个 UI 实测问题拆到可验证的服务端合同、组件测试和浏览器场景，防止后续以改文案掩盖业务问题。

## 实际完成内容

- 新增 `docs/项目计划书v0/项目计划v0.10/02-v0.10问题场景与验收映射.md`。
- 状态绕过、回收站、真实板块范围、权限越权、个人主页可见性和 API Index 使用 Go 测试覆盖。
- 插件中心的已发布目录、用户申请入口由 Web 组件测试覆盖。
- 七视口 Playwright 工作流覆盖 Web 与 Admin 的关键页面和横向溢出检查。

## 主要修改目录和文件

- `internal/community/service/*_test.go`
- `internal/richtext/service_test.go`
- `internal/core/identity/service/permission_service_test.go`
- `internal/space/sync_test.go`
- `internal/transport/httpapi/api_index_test.go`
- `web/tests/plugin-center.spec.ts`
- `web/tests/responsive-workflow.mjs`

## 架构、兼容与迁移

测试只描述用户可见行为和服务端不变量，不要求修改旧接口或数据。

## 测试与构建结果

- `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` 通过。
- `pnpm --dir web test:component` 通过。
- 七视口浏览器工作流通过，详情见 `v0.10.21-dev.md`。

## 回滚方式

测试和映射文档可独立回退，不影响运行时数据。

## 已知限制

浏览器工作流验证关键路径，不替代真实高并发、跨浏览器和无障碍人工验收。

## 下一阶段任务

完成架构、数据库、目录、生成物和兼容对象清点。

