# CampusOS v0.10.3-dev：A2 当前系统清点与治理边界

> 日期：2026-07-16  
> 实现状态：已完成  
> 验收状态：已在 A15 的统一门禁复验

## 阶段目标

为目录、表、路由和兼容对象建立可检查的事实源，禁止在没有所有权与恢复证据时清理历史对象。

## 实际完成内容

- 新增 `docs/architecture/v10当前系统清点与治理.md`，说明模块、表、读写路径、保留策略、目录与 Resource Package 边界。
- 更新 `docs/architecture/当前架构概览.md` 和 Admin `/architecture`，展示 v0.10 的 `000023` 至 `000025`、内容治理和权限目录对象。
- 新增 `scripts/check-data-governance.py`，机器检查插件实现、插件数据、用户空间、资源仓库和 Legacy Resource Source 的边界。
- 新增 `scripts/check-generated-files.py`，禁止跟踪 Web/Admin/docs-site 构建产物；将 `admin/dist/` 和 docs-site 临时目录加入忽略策略。

## 主要修改目录和文件

- `docs/architecture/`
- `admin/src/modules/architecture/pages/SystemArchitectureView.vue`
- `scripts/check-data-governance.py`
- `scripts/check-generated-files.py`
- `.gitignore`、`Makefile`

## 架构、兼容与迁移

- `data/plugins` 只保存插件实现；`data/plugin_data` 保留运行数据与 Legacy 风格包来源；`data/resources` 是无 Runtime 的资源仓库。
- `user_space_contents`、旧 `permissions`、Legacy style source 均标记为兼容期对象，不执行物理删除。

## 测试与构建结果

- `make data-governance-check` 通过。
- `make generated-files-check` 通过。
- `make architecture-check` 与 `make docs-links` 通过。

## 回滚方式

删除检查器或恢复忽略规则不会影响业务数据；被取消追踪的构建产物可从源码重新生成。

## 已知限制

本阶段没有把 Legacy Resource Source 物理迁移到 `data/resources`，因为导入、统计和回滚证据尚未满足。

## 下一阶段任务

引入内容三维状态、修订和审核审计数据结构。

