# CampusOS v0.10.20-dev：A14 数据库治理与弃用收口

> 日期：2026-07-16  
> 实现状态：已完成  
> 验收状态：已在 A15 的统一门禁复验

## 阶段目标

说明所有 v0.10 新表、兼容表和文件目录的所有者、读写路径、保留策略与恢复要求，并阻止无证据清理。

## 实际完成内容

- 架构清点文档记录 Identity、Community、RichText、Personal Space、Plugin Platform 和集成表的归属。
- `database-audit.sql` 增加 v0.10 三维内容状态、内容审计孤儿、权限目录和路由绑定的完整性检查。
- `schema-contract.sql` 要求 v0.10 表、列、检查约束和关键索引存在。
- Admin 数据架构页同步显示 `000023`、`000024`、`000025` 与相应逻辑关系。

## 主要修改目录和文件

- `docs/architecture/v10当前系统清点与治理.md`
- `scripts/database-audit.sql`
- `scripts/schema-contract.sql`
- `admin/src/modules/architecture/pages/SystemArchitectureView.vue`
- `migrations/000023_*`、`000024_*`、`000025_*`

## 架构、兼容与迁移

- 历史 migration 保持只读；v0.10 均为顺序追加。
- `permissions`、`user_space_contents` 和 Legacy resource source 仍在弃用/比对期，未做物理删除。

## 测试与构建结果

- `make migrate-status` 显示 `000001` 至 `000025` 已应用。
- `make database-check` 通过，审计违规数为零且 v0.10 schema contract 满足。

## 回滚方式

开发/演练环境可验证 down migration；生产优先 forward-fix，并在迁移前备份 PostgreSQL 与 `data/` 联合数据。

## 已知限制

内容修订、审核记录、插件日志和集成投递的长期保留期限需要以真实容量与合规要求另行制定。

## 下一阶段任务

运行全链路发布门禁、恢复演练和七视口浏览器回归，完成 v0.10 关闭审查。

