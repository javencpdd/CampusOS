# CampusOS v0.10.19-dev：A13 项目目录与生成物治理

> 日期：2026-07-16  
> 实现状态：已完成  
> 验收状态：已在 A15 的统一门禁复验

## 阶段目标

让仓库可以机器识别插件实现、插件运行数据、用户文件、资源包和重新可生成产物的边界。

## 实际完成内容

- `data-governance-check` 检查 `data/plugins`、`data/plugin_data`、`data/personal-space`、`data/resources` 及七类资源目录。
- 资源仓库禁止出现 `plugin.yaml`、`go.mod`、`Cargo.toml` 等完整插件实现标识。
- `generated-files-check` 检查 `.gitignore` 并拒绝 Git 继续跟踪 `web/dist`、`admin/dist` 与 docs-site 构建缓存/输出。
- 移除 Admin 历史构建产物的索引跟踪，保留本地文件并允许从源码重新构建。

## 主要修改目录和文件

- `scripts/check-data-governance.py`
- `scripts/check-generated-files.py`
- `.gitignore`
- `Makefile`
- `scripts/release-check.sh`
- `data/resources/`

## 架构、兼容与迁移

- Legacy 风格包仍从 `data/plugin_data/*/style-packs` 兼容读取，未进行不可逆目录移动。
- `data/resources` 只保存 Resource Package；它不允许业务 Runtime、路由注册或 migration。

## 测试与构建结果

- `make data-governance-check` 通过。
- `make generated-files-check` 通过。
- 干净构建行为由最终 release-check 覆盖。

## 回滚方式

生成物可重新构建；目录策略和检查器可独立回退。没有删除插件数据、用户文件或历史资源包。

## 已知限制

未将所有 Legacy 风格包物理迁移到 `data/resources`，因为尚无完整导入、版本比对和回滚链路。

## 下一阶段任务

完成数据库表所有权、保留策略、迁移和恢复证据的收口。

