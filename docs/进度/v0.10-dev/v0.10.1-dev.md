# CampusOS v0.10.1-dev：A0 基线冻结与动态复验

> 日期：2026-07-16  
> 实现状态：已完成  
> 验收状态：已在 A15 的统一门禁复验

## 阶段目标

在进入内容、权限和目录迁移前，冻结可重复的 v0.10 起点，避免把 v0.9 的历史结果冒充为本次证据。

## 实际完成内容

- 核对顺序 migration，v0.10 只追加 `000023`、`000024`、`000025`。
- 固定 Web、Admin、docs-site、API 与数据库的本地运行入口，并保留 `STOP_EXISTING=true make dev-all` 作为动态复验命令。
- 以 Route Contract、OpenAPI、权限目录、数据目录与架构清点作为本次变更前后的兼容对照。
- 将最终数据库检查、恢复演练和浏览器 smoke 纳入 `make release-check`。

## 主要修改目录和文件

- `scripts/release-check.sh`
- `scripts/database-audit.sql`
- `scripts/schema-contract.sql`
- `docs/进度/v0.10-dev/`

## 架构、兼容与迁移

- 不改写 `000001` 至 `000022`；所有新数据模型均由追加 migration 建立。
- 已有 API 路径、旧 `permissions` 表、Legacy style source 和 `runtime: builtin` 兼容路径继续保留。

## 测试与构建结果

- `make migrate-status`：`000001` 至 `000025` 已应用。
- `make database-check`：数据库审计与 schema contract 通过。
- 最终整体验证、恢复演练和浏览器 smoke 见 `v0.10.21-dev.md`。

## 回滚方式

本阶段没有破坏性改动。遇到问题先停止后续 migration 的使用并保留快照；生产回退优先使用前向修复，不以删除用户数据作为回滚手段。

## 已知限制

基线是本地开发环境证据，不等同于跨节点生产容量或灾备演练。

## 下一阶段任务

建立十个实测问题与测试、模块和验收门禁的映射。

