# v0.8.16-dev：A16 文档、最终验收与 v0.8.0 封版

> 阶段状态：已完成，已验收
> 更新日期：2026-07-13
> 对应计划：[v0.8 现有功能架构收敛计划书](../../项目计划书v0/项目计划v0.8/00-v0.8版本计划书.md)

## 阶段目标

完成 v0.8 的版本一致性、计划状态、进度记录和最终验收，使架构收敛结果可追踪、可验证、可回滚。

## 实际完成内容

- 将服务启动日志、健康检查、官方文档包和根 README 的版本基线更新为 `v0.8.0`。
- 插件 CLI 新建模板的 CampusOS 兼容范围更新为 `>=0.6.0 <0.9.0`，保持旧插件包与 v0.8 兼容。
- 完成 A0-A16 状态同步，新增 v0.8 架构收敛回顾与验收文档。
- 发布级验证覆盖 API 合同、数据库 20 个 migration/31 张表同步、恢复演练、插件模板、Web/Admin/docs-site/SDK 构建与浏览器 smoke。

## 主要修改

- `cmd/server/main.go`
- `internal/core/identity/handler/user_handler.go`
- `cmd/campusosctl/main.go`
- `README.md`
- `docs-site/package.json`
- `docs/项目计划书v0/项目计划v0.8/*`
- `docs/进度/v0.8-dev/*`

## 兼容与回滚

HTTP API 仍使用既有 `v0.6` 合同名称和 Host API v1；版本提升不修改 migration、表、数据目录、风格包或 Manifest。若需要回滚运行版本，恢复上一个二进制即可，现有 v0.8 数据继续兼容。

## 最终验收

```bash
make architecture-check
GOCACHE=/tmp/campusos-go-cache go test ./... -count=1
make frontend-budget
RUN_RESTORE_DRILL=true RUN_BROWSER_SMOKE=true make release-check
git diff --check
```

均已通过。最终回顾见 [v0.8 架构收敛回顾与验收](../../项目计划书v0/项目计划v0.8/01-v0.8架构收敛回顾与验收.md)。

## 后续范围

Agent Center、Agent Runner、标准 MCP Server、Knowledge Provider、第三方 Adapter、插件市场和生产级高可用仍属于后续独立版本范围，不在 v0.8 中实现。
