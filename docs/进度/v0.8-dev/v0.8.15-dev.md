# v0.8.15-dev：A15 稳定性、性能与故障验证

> 阶段状态：已完成，已验收
> 更新日期：2026-07-13
> 对应计划：[v0.8 现有功能架构收敛计划书](../../项目计划v8/00-v8版本计划书.md)

## 阶段目标

验证 v0.8 主路径在并发、基础设施回退、迁移/恢复、前端状态、浏览器渲染和构建体积下保持可用，并将预算纳入自动化检查。

## 实际完成内容

- 对 Module Kernel、Feature Registry、User Storage、Plugin、Space、RichText、Schedule 运行 race 检查。
- 完整 Go 测试、架构/合同检查、数据库检查、数据架构同步、插件模板检查和 Git whitespace 检查均通过。
- 发布检查执行 isolated restore drill 和浏览器 smoke；重启开发栈后，健康检查、首页外观配置和主题目录均成功返回。
- `style-pack-responsive.mjs` 在 PC/移动端风格包流程中通过。
- 新增 `scripts/check-frontend-bundles.py` 与 `make frontend-budget`，并接入 `release-check`。

## 预算结果

| 应用 | JS Entry | JS 预算 | CSS Entry | CSS 预算 |
| --- | ---: | ---: | ---: | ---: |
| Web | 1,264,920 bytes | 1,350,000 | 362,298 bytes | 400,000 |
| Admin | 1,054,075 bytes | 1,150,000 | 357,503 bytes | 400,000 |

Vite 对 Element Plus 相关主 chunk 仍给出默认 500 KiB 建议；固定预算已作为发布门禁，当前产物在预算内。

## 验证

```bash
GOCACHE=/tmp/campusos-go-cache go test -race ./internal/platform/module ./internal/platform/feature ./internal/core/storage ./internal/plugin ./internal/space ./internal/richtext ./internal/schedule -count=1
GOCACHE=/tmp/campusos-go-cache go test ./... -count=1
make architecture-check
make frontend-budget
RUN_RESTORE_DRILL=true RUN_BROWSER_SMOKE=true make release-check
STOP_EXISTING=true make dev-all
```

均已通过。

## 回滚与限制

验证不修改用户业务数据；恢复演练在隔离数据库和临时目录中运行。预算是当前依赖集合的上限，不代表不需要继续按需拆分 Element Plus 与编辑器依赖。

## 下一阶段

执行 A16：同步最终版本、计划、回顾文档和发布验收记录。
