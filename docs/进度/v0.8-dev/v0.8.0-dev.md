# v0.8.0-dev：A0 基线与所有权冻结

> 阶段状态：已验收  
> 完成日期：2026-07-13  
> 对应计划：[v0.8 现有功能架构收敛计划书](../../项目计划v0.8/00-v0.8版本计划书.md)

## 阶段目标

在移动任何业务代码、表、路由或数据目录前，冻结 v0.8 的真实兼容基线，并为现有功能建立唯一责任领域。该阶段只新增架构证据，不改变运行时行为。

## 实际完成内容

- 新增机器可读基线 [`v0.8-current-system-baseline.json`](../../architecture/v0.8-current-system-baseline.json)，记录当前 Git 基线、148 条 HTTP 合同 checksum、18 个 migration、30 张 schema 表、9 个 Manifest、8 个数据根目录、前端入口和 8 项历史架构例外。
- 新增 [`v0.8功能所有权矩阵.md`](../../architecture/v0.8功能所有权矩阵.md)，覆盖 Identity、Community、Moderation、User Storage、三个 Built-in Feature、Appearance、Plugin Platform、现有集成、观测和平台配置能力。
- 明确 A0 的迁移规则：既有 API、历史 migration、Manifest、文件根目录、用户文件 URL、Web/Admin URL 和权限结果保持兼容；后续改造必须以单向 compatibility Adapter 迁移。

## 主要修改目录和文件

- `docs/architecture/v0.8-current-system-baseline.json`
- `docs/architecture/v0.8功能所有权矩阵.md`
- `docs/项目计划v0.8/00-v0.8版本计划书.md`
- `docs/进度/v0.8-dev/v0.8.0-dev.md`

## 架构变化

没有运行时代码、数据库或路由变化。A0 将原先分散在 v0.7 基线、服务器装配和路由文件中的事实固化为 v0.8 的迁移约束：`internal/server/application.go` 是待拆解的兼容 Composition Root，而不是后续业务的扩展入口。

## 兼容和迁移处理

- 未修改 `000001` 至 `000018` migration、数据库数据、`data/*` 目录或 9 个插件 Manifest。
- 未修改 HTTP/OpenAPI/权限合同，已记录其 SHA-256，后续合同漂移必须有显式兼容说明。
- 未修改 Web/Admin URL、localStorage、用户文件路径、富文本、课表或风格包数据。

## 测试与构建结果

2026-07-13 执行并通过：

```bash
GOCACHE=/tmp/campusos-go-cache go test ./... -count=1
make architecture-check
make contracts-check
make docs-links
git diff --check
RUN_RESTORE_DRILL=true RUN_BROWSER_SMOKE=true \
  STYLE_PACK_SCREENSHOT_DIR=/tmp/campusos-v08-a0 make release-check
```

`release-check` 覆盖 database audit/schema contract、builtin/gRPC/Wasm 模板、Web/Admin/docs-site/SDK 构建、隔离恢复演练和浏览器流程。架构检查当前以 8 项既有例外通过；该数已成为 A1 起只减不增的基线，不代表债务已清除。

## 回滚方式

本阶段为文档和机器快照的纯加法。回滚只需删除 A0 新增文档，不涉及 migration down、数据恢复或运行时切换。

## 已知限制

- Composition Root、重复 PG/Memory 装配、集中路由和前端集中结构仍存在，尚未迁移。
- 基线中存在的 8 项跨 Repository 例外仍受 allowlist 管理；A14 前不得新增。
- A0 不建立新的 Feature Store 或 Resource 数据迁移，避免未经 Runtime/Profile 边界验证就引入双主路径。

## 下一阶段任务

A1 将固化 Module/Route/Port/Adapter/compatibility 贡献规范，扩展架构边界检查器，并将 allowlist 从“说明性清单”升级为不可增加的自动化门禁。
