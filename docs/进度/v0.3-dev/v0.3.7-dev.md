# CampusOS v0.3.7-dev 进度说明

> 日期：2026-06-28
> 状态：已完成

## 1. 本次目标

完成 v0.3-dev 首轮 P0 中的 `examples/plugins/hello-wasm` 示例插件，让当前 Wasm Runtime 有一个可直接安装、启动、接收事件并写入插件日志的最小样例。

本次遵循现有 v0.3-dev Wasm ABI：导出无参数函数 `handle_event`，返回非 0 表示允许事件继续。

## 2. 完成内容

| 项目 | 结果 |
| --- | --- |
| 示例目录 | 新增 `examples/plugins/hello-wasm` |
| Manifest | 新增 `plugin.yaml`，声明 `runtime: wasm`、事件订阅和 Wasm 配置 |
| Wasm 模块 | 新增预构建 `plugin.wasm`，可被当前 Runtime 直接实例化 |
| 可读源文件 | 新增 `src/handle_event.wat` 和 `src/plugin.wasm.hex` |
| 使用说明 | 新增 `README.md`，说明当前 ABI、文件结构和重新生成方式 |
| 集成测试 | 新增测试验证 hello-wasm 可安装、启停、接收事件并写入日志 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `examples/plugins/hello-wasm/plugin.yaml` | hello-wasm 插件 manifest |
| `examples/plugins/hello-wasm/plugin.wasm` | 预构建 Wasm 模块 |
| `examples/plugins/hello-wasm/README.md` | 示例插件说明 |
| `examples/plugins/hello-wasm/src/handle_event.wat` | Wasm 文本源文件 |
| `examples/plugins/hello-wasm/src/plugin.wasm.hex` | `plugin.wasm` 十六进制表示 |
| `internal/plugin/wasm/example_test.go` | hello-wasm 生命周期与日志集成测试 |
| `docs/进度/v0.3-dev/v0.3.7-dev.md` | 本次进度说明 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `xxd -r -p examples/plugins/hello-wasm/src/plugin.wasm.hex examples/plugins/hello-wasm/plugin.wasm` | 通过 |
| `file examples/plugins/hello-wasm/plugin.wasm` | 确认为 WebAssembly binary module |
| `go test ./internal/plugin/wasm -run TestHelloWasmExampleLifecycleAndLogging -count=1 -v` | 通过 |
| `go test ./...` | 通过 |
| `git diff --check` | 通过 |

本次未运行前端构建，因为没有修改 `web` 或 `admin` 代码。

本次未运行 migration 验证，因为没有修改 `migrations`。

## 5. 后续任务

| 优先级 | 任务 |
| --- | --- |
| P0 | 设计事件 payload ABI，将 `EventMessage` 安全传入 Wasm |
| P0 | 将 Host API HTTP 服务正式接入插件运行时或 SDK 调用路径 |
| P1 | 增加 Go SDK 或插件构建工具，减少手写 Wasm 样例成本 |
| P1 | 管理后台增加插件日志查看入口 |
