# CampusOS v0.3.10-dev 进度说明

> 日期：2026-06-30
> 状态：已完成

## 1. 本次目标

完成 v0.3-dev 剩余任务中的管理后台插件日志入口，让管理员可以在插件管理页直接查看单个插件的运行日志。

## 2. 完成内容

| 项目 | 结果 |
| --- | --- |
| 后端查询能力 | `Manager` 新增 `ListPluginLogs`，复用当前 `PluginLogRepository` |
| HTTP API | 新增 `GET /api/v1/plugins/:name/logs?limit=100` |
| 权限控制 | 插件日志接口沿用插件管理权限 `role/manage` |
| Admin API | `pluginApi.logs(name, { limit })` 封装插件日志请求 |
| Admin 页面 | 插件管理表格新增“日志”按钮，打开运行日志对话框 |
| 日志展示 | 展示时间、级别、消息、事件类型和 metadata 详情 |
| 构建产物 | 因 `admin/dist` 已被仓库跟踪，本次同步刷新 admin 构建产物 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `internal/plugin/manager.go` | 增加插件日志读取方法 |
| `internal/plugin/handler.go` | 增加插件日志 HTTP handler |
| `internal/server/server.go` | 注册插件日志管理路由 |
| `internal/plugin/runtime_contract_test.go` | 增加插件日志读取测试 |
| `admin/src/api/index.ts` | 增加插件日志 API 封装 |
| `admin/src/views/PluginManageView.vue` | 增加插件日志查看对话框 |
| `admin/dist` | 刷新 admin 构建产物 |
| `docs/进度/v0.3-dev/v0.3.10-dev.md` | 本次进度说明 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `go test ./internal/plugin ./internal/server -count=1` | 通过 |
| `cd admin && pnpm build` | 通过 |
| `go test ./...` | 通过 |
| `git diff --check` | 通过 |

本次未运行 `cd web && pnpm build`，因为没有修改 `web` 代码。

本次未运行 migration 验证，因为没有修改 `migrations`。

## 5. 后续任务

| 优先级 | 任务 |
| --- | --- |
| P1 | 完成插件 SQLite 存储设计文档 |
| P2 | 增加 Go SDK 雏形 |
| P2 | 增加 `campusosctl plugin init/inspect` 最小 CLI |
| P2 | 增加 Windows CI matrix |
