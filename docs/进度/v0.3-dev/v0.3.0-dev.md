# CampusOS v0.3.0-dev 进度说明

> 日期：2026-06-28
> 状态：已完成

## 1. 本次目标

完成 v0.3-dev 第一轮的 Runtime 接口复核，确认现有插件管理器可以继续作为 gRPC Runtime 与后续 Wasm Runtime 的统一生命周期入口。

同时创建可复用的 CampusOS v0.3-dev 开发流程 skill，固化“完成任务、更新 `v0.3.*-dev.md` 文档、验证、commit”的工作方式。

## 2. 完成内容

| 项目 | 结果 |
| --- | --- |
| Runtime 接口复核 | 保持 `internal/plugin/runtime.go` 接口稳定，暂不做破坏性改动 |
| Manifest wasm 支持 | 新增测试确认 `runtime: wasm` 可以被 Manifest 接受 |
| Manager 生命周期契约 | 新增测试确认 Manager 通过注册的 Runtime 完成 Enable/Start/Stop |
| Runtime 缺失处理 | 新增测试确认插件声明未注册 Runtime 时会进入 `error` 状态 |
| v0.3-dev 流程 skill | 已创建 `/home/jack/.codex/skills/campusos-v03-dev-workflow` 并通过 quick_validate |
| skill 使用说明 | 已写入 `docs/项目计划v3/CampusOS-v0.3-dev流程Skill使用说明.md` |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `internal/plugin/runtime_contract_test.go` | 新增 Runtime/Manager/Manifest 契约测试 |
| `docs/进度/v0.3-dev/v0.3.0-dev.md` | 本次进度说明 |
| `docs/项目计划v3/CampusOS-v0.3-dev流程Skill使用说明.md` | v0.3-dev 流程 skill 使用说明 |

仓库外新增：

| 路径 | 说明 |
| --- | --- |
| `/home/jack/.codex/skills/campusos-v03-dev-workflow` | 可复用 Codex skill |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `python3 /home/jack/.codex/skills/.system/skill-creator/scripts/quick_validate.py /home/jack/.codex/skills/campusos-v03-dev-workflow` | 通过 |
| `git diff --check` | 通过 |
| `go test ./...` | 通过 |
| `/home/jack/.codex/skills/campusos-v03-dev-workflow/scripts/check_v03_task.sh /home/jack/bbs/bbs01/CampusOS` | 通过 |

本次未运行前端构建，因为没有修改 `web` 或 `admin` 代码。

## 5. 后续任务

| 优先级 | 任务 |
| --- | --- |
| P0 | 新增 `internal/plugin/wasm` Runtime 骨架 |
| P0 | 实现 Wasm 插件启动、停止、健康检查的最小闭环 |
| P0 | 为 Wasm 调用增加超时和错误隔离 |
| P0 | 接入 Host API 权限检查 |
