# CampusOS v0.3.19-dev 进度说明

> 日期：2026-06-30
> 状态：已完成

## 1. 本次目标

回顾当前仓库从 v0.1、v0.2 到 v0.3-dev 已完成的更新，对根目录 `README.md` 做一次详细同步，使新开发者可以从 README 直接了解当前项目状态、启动方式、插件能力、CI/CD 和跨平台边界。

## 2. 完成内容

| 项目 | 结果 |
| --- | --- |
| 项目状态 | 更新为当前 v0.3-dev 状态，覆盖前台、后台、后端、迁移、插件、SDK、CLI、CI/CD 和 Windows 兼容状态 |
| 版本回顾 | 补充 v0.1、v0.2、v0.3-dev 前置稳定化和 v0.3-dev 首轮任务完成情况 |
| 架构说明 | 更新为当前 web/admin/API/Host API/Plugin Runtime/Docker 服务结构 |
| 快速开始 | 按 `.env.example`、`make docker-up`、`make migrate-up`、`make run`、前后台启动流程重写 |
| 迁移机制 | 说明 `make migrate-up` 通过脚本扫描 `migrations/*.up.sql` 和 `schema_migrations` 工作，不再写死 migration 文件 |
| 插件系统 | 补充 Wasm Runtime、payload ABI、Host API、权限校验、插件日志和示例插件说明 |
| SDK/CLI | 增加 `sdk/go` 与 `cmd/campusosctl` 的当前能力和使用示例 |
| CI/CD | 同步当前 GitHub Actions 的 backend、frontend、windows-go 和 deploy 流程 |
| 跨平台说明 | 明确 Ubuntu/WSL2 推荐路径、原生 Windows 难点，以及暂不建议直接以双系统完整兼容作为主线开发目标 |
| 限制与后续 | 列出 `SetConfig` 持久化、SQLite 存储、插件打包、SDK harness、Windows 实机验证等后续重点 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `README.md` | 按当前 v0.3-dev 真实状态重写项目说明 |
| `docs/进度/v0.3-dev/v0.3.19-dev.md` | 本次 README 更新进度说明 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `git diff --check` | 通过 |
| `go test ./...` | 通过 |

本次只修改文档，未修改 Go、前端或 migration 代码。仍按 v0.3-dev 工作流执行基础校验。

## 5. 后续任务

| 优先级 | 任务 |
| --- | --- |
| P1 | 根据后续 v0.3-dev 代码进展持续同步 README 的插件能力和限制 |
| P1 | 在真实 Windows + Docker Desktop 环境完成 `.ps1` 脚本验证后更新 README 跨平台说明 |
| P2 | 当 `campusosctl plugin pack/install` 落地后补充 CLI 使用说明 |
