# CampusOS v0.3.17-dev 进度说明

> 日期：2026-06-30
> 状态：已完成

## 1. 本次目标

将 Go SDK 的 Host API client 与当前 v0.3-dev Host API 契约对齐，补齐 `GetThread`、`GetReply`、`CheckPermission`、`Log` 等方法的便捷封装。

## 2. 完成内容

| 项目 | 结果 |
| --- | --- |
| 内容查询 | 新增 `GetUser`、`GetThread`、`GetReply`、`QueryThreads` |
| 事件/通知 | 新增 `PublishEvent`、`SendNotification` |
| 配置 | 保留 `GetConfig/GetAllConfig`，新增 `SetConfig` |
| 权限 | 新增 `CheckPermission` |
| 日志 | 新增 `Log` |
| 存储 | 保留 `StorageGet`、`StorageSet`、`StorageDelete` |
| 文档 | 更新 `sdk/go/README.md` 当前能力表 |
| 测试 | 增加 GetThread、CheckPermission、Log 请求封装测试 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `sdk/go/hostapi.go` | 增加 Host API client 方法和请求/响应结构 |
| `sdk/go/hostapi_test.go` | 增加新增 SDK 方法测试 |
| `sdk/go/README.md` | 同步 SDK 能力说明 |
| `docs/进度/v0.3-dev/v0.3.17-dev.md` | 本次进度说明 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `go test ./sdk/go -count=1 -v` | 通过 |
| `go test ./...` | 通过 |
| `git diff --check` | 通过 |

本次未运行前端构建，因为没有修改 `web` 或 `admin` 代码。

本次未运行 migration 验证，因为没有修改 `migrations`。

## 5. 后续任务

| 优先级 | 任务 |
| --- | --- |
| P1 | 为 SDK 增加本地插件测试 harness |
| P1 | 为 SDK 增加 Wasm 编译模板或 TinyGo 示例 |
| P2 | 在 `campusosctl` 中增加 `plugin pack` |
