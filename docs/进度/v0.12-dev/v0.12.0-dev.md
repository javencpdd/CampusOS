# CampusOS v0.12.0-dev 进度说明

> 日期：2026-07-17
> 阶段：A0 计划与范围冻结
> 状态：已验收（仅文档范围）

## 1. 本阶段目标

读取 `docs/项目计划v12/v12计划思路.md`，结合当前 v11 代码、模块、数据库、前后端、
生成合同和发布门禁，形成可分阶段实施、可验证和可回滚的正式 v12 计划。

## 2. 实际完成内容

- 核验当前分支 `djw-update`、提交 `241b723`、应用版本 `v0.11.0` 和 migration
  `000001-000027`。
- 核验当前 8 个 Core Module、10 个 Built-in Feature 和 205 条生成 HTTP 路由。
- 确认 Identity 的重复邮箱事实、未消费验证状态、无状态 Refresh JWT、未使用 Session
  表和默认管理员密码日志问题。
- 确认 Category 已有 `parent_id/is_closed`，但缺少层级、版本、可靠命令和 `color` 后端
  合同。
- 确认普通帖子创建未进入 Reliability，RichText 仍采用 Thread 回收站补偿且 Store
  不能参与 TxKernel。
- 将 v12 收敛为可信账号、两级板块、结构化帖子三条串行主线，并定义阶段、迁移、
  兼容、回滚、测试、文档和最终验收标准。

## 3. 主要修改文件

- `docs/项目计划v12/00-v12版本计划书.md`
- `docs/进度/v0.12-dev/v0.12.0-dev.md`

未修改运行时代码、API、数据库 migration、配置或版本号。

## 4. 架构变化

本阶段没有运行时架构变化。计划确定后续新增 `core.email-delivery`，继续复用
`core.reliability`，并通过 Community 编译内事务 Port 协调结构化帖子；External Plugin
仍不能访问 AppContext、TxKernel 或核心数据库。

## 5. 兼容和迁移处理

- 当前 v11 行为保持不变。
- 正式计划明确历史账号使用 `legacy_accepted`，不伪装成 verified。
- 明确 v12 将有两项受控安全不兼容：旧 JWT 强制重新登录；旧一步式注册改为稳定的
  verification-required 错误。
- 所有数据库变化从 `000028` 追加，历史 migration 不修改。

## 6. 测试与构建结果

本阶段是文档规划，不重复执行 v11 的数据库、恢复和浏览器全量发布门禁。已执行：

| 命令 | 结果 |
| --- | --- |
| `make docs-links` | 通过，校验 176 个本地链接。 |
| `GOCACHE=/tmp/campusos-go-cache make contracts-check` | 通过，205 条路由对应的 JSON、Markdown、OpenAPI 和插件权限生成物一致。 |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过，全量 Go package 无失败。 |
| `make architecture-check` | 通过，后端/前端模块边界及 9 个检查器测试无失败。 |
| `make reliability-check` | 通过，TxKernel、持久 Worker 和 User Storage 边界保持有效。 |
| `git diff --check` | 通过。 |

A0.5 将重新执行冷启动、前端构建、数据库、恢复和浏览器门禁，并把本次 Go/合同结果
纳入可重复的 v12 独立基线。

## 7. 回滚方式

删除本次新增的正式计划和 A0 进度文档即可回到原思路归档；不会影响任何运行时或数据。
正常情况下保留 `v12计划思路.md` 作为来源记录，`00-v12版本计划书.md` 作为后续唯一
执行基线。

## 8. 已知限制

- 当前只完成计划，不代表邮箱、Session、两级板块或结构化帖子已经实现。
- 当前 Onboarding Skill 的状态快照停留在旧版本，不能作为 v12 验收证据。
- A0.5 尚未完成，因此计划中引用的当前测试状态只能沿用 v11 证据，不能标记为 v12
  基线。

## 9. 下一阶段任务

执行 A0.5：从当前代码冷启动四个服务，冻结 Go/Web/Admin/docs-site/SDK、205 条路由、
migration、模块/插件/资源、数据目录、数据库、恢复和浏览器回归证据。基线有失败时先
修复回归，不进入默认管理员和 Identity 改造。
