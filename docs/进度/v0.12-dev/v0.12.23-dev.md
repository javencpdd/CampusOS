# CampusOS v0.12.23-dev 本地 QQ/Foxmail SMTP 启用说明

> 日期：2026-07-19
> 状态：已完成

## 1. 本次目标

把当前本地 development 环境从 `EMAIL_PROVIDER=fake` 切换到 QQ/Foxmail SMTP，并通过真实注册
Challenge 验证可靠事件和邮件 Consumer 可以完成投递。

## 2. 完成内容

- 在被 Git 忽略的本地 `.env` 中配置 `smtp.qq.com:587`、STARTTLS、完整 SMTP 用户名、裸发件地址和
  授权码；本文档及版本库不记录邮箱、授权码或验证码。
- 将 `.env` 权限收紧为 `0600`，并确认 `.gitignore` 会排除该文件。
- 使用 `STOP_EXISTING=true make dev-all` 重启 API、Web、Admin 和 docs-site，SMTP 配置成功通过启动校验。
- 创建一枚新的注册 Challenge，HTTP 返回 `200`；对应 Outbox 事件一次处理后为 `published`。
- `core.email-delivery.challenge` Consumer Receipt 已落库，Attempt 为 `succeeded` 且没有错误信息；
  `event:fallback` 和 `system:outbox-finalize` 也均成功。

## 3. 修改范围

- 本地运行配置：`.env`（已忽略、权限 `0600`，不进入 Git）
- 进度记录：`docs/进度/v0.12-dev/v0.12.23-dev.md`

没有修改源码、migration、数据库结构、API 合同或用户数据。验证过程中只创建了一枚按正常生命周期过期的
注册 Challenge 及其限流、审计、Outbox 和 Consumer Receipt 记录。

## 4. 验证结果

| 检查 | 结果 |
| --- | --- |
| `.env` 文件权限 | `0600`。 |
| `git check-ignore -v .env` | 通过，命中 `.gitignore`。 |
| `STOP_EXISTING=true make dev-all` | 通过，四端就绪。 |
| `POST /api/v1/auth/registration/challenge` | `200`，返回不透明 Challenge ID。 |
| 最新邮件 Outbox | `published`，`attempts=1`，无 `last_error`。 |
| 邮件 Consumer Attempt | `succeeded`，已写 Consumer Receipt。 |
| `git diff --check` | 通过。 |

## 5. 安全与后续

本次使用的授权码此前已经通过非 Secret 通道暴露，虽然技术投递验证成功，仍应立即在邮箱后台撤销并生成
新授权码。更新本地 `.env` 后再次重启服务；不得把新授权码发送到聊天、写入进度文档或提交到 Git。
