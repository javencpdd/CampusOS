# CampusOS v0.12.1-dev 进度说明

> 日期：2026-07-17
> 阶段：A0.5 v11 基线冻结
> 实现状态：完成
> 验收状态：已验收

## 1. 本阶段目标

从当前提交 `241b72302b9ab1d087e58d0cfd9424bf8b0d1efb` 冷启动 CampusOS，冻结 v12
实施前的运行、数据库、路由、模块、资源和恢复证据。该阶段不改变任何业务行为。

## 2. 实际完成内容

- 使用 `STOP_EXISTING=true make dev-all` 停止旧监听进程并从当前工作树重新启动 API、Web、
  Admin 和 docs-site；四个端点均返回成功状态。
- 冻结 HTTP 路由合同：205 条路由，`http-routes-v0.6.json` SHA-256 为
  `7c8ebc3439cbde3ab0d5fc864d3274652fd8f472497f87dc76be04e0ab3bf8d5`，OpenAPI SHA-256 为
  `6a67362bfc3a8ca4efede7e3e681eab3a1c0b69a19f4370a40ac93e54bda7549`。
- 冻结模块和资源清单：8 个 Core Module、10 个 Built-in Feature、27 个已提交 migration、
  2 个外部插件目录和 5 个 Resource Package。
- 冻结当前数据目录结构：`data/module_data`、`data/plugin_data`、`data/plugins`、
  `data/resources`、`data/personal-space`、`data/images`、`data/config`、`data/dist` 和
  `data/skills` 均存在；当前 `data/plugin_data` 没有顶层插件数据目录。
- 对当前 PostgreSQL 执行 migration status、数据库审计、schema contract、备份恢复演练和
  浏览器回归，确认没有把旧进程或旧 migration 误当作 v12 证据。

## 3. 主要修改文件

- `docs/项目计划v12/00-v12版本计划书.md`
- `docs/进度/v0.12-dev/v0.12.1-dev.md`

本阶段没有修改运行时代码、迁移、API、配置、数据或应用版本。

## 4. 架构变化

无运行时架构变化。基线确认后续 v12 可以继续使用现有 Module Registry、Feature Gate、
TxKernel、Reliability Outbox 和模块/插件/资源目录隔离，不需要在 A1 重做基础设施。

## 5. 兼容和迁移处理

- 数据库当前应用 `000001` 至 `000027`；v12 的所有 schema 改动必须从 `000028` 追加。
- 当前 API 健康端点报告 `v0.11.0`，Web/Admin/docs-site 均从本工作树启动并返回 `200`。
- A0.5 不改认证、路由和数据，因此回滚只需删除本阶段文档；不执行数据库 down。

## 6. 测试与构建结果

| 命令或检查 | 结果 |
| --- | --- |
| `STOP_EXISTING=true make dev-all` | 通过；API `8080`、Web `3000`、Admin `3001`、docs-site `3002` 全部就绪。 |
| `GOCACHE=/tmp/campusos-go-cache RUN_RESTORE_DRILL=true RUN_BROWSER_SMOKE=true make release-check` | 通过；包含合同、文档、架构、可靠性、Go、数据库、插件、资源、三端构建、SDK、恢复和浏览器流程。 |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过；由 release-check 执行。 |
| Web/Admin/docs-site/SDK build | 通过；Admin 组件测试 6/6 通过。 |
| `make database-check` 与架构同步 | 通过；数据库审计 0 个违规，schema contract 为 `v0.11-reliability-v2`，54 张表同步。 |
| isolated restore drill | 通过；恢复了数据库、模块、插件、资源和用户资产。 |
| 浏览器、风格包和七视口 smoke | 通过；认证、帖子、空间、权限、Admin、文档和 7 个视口均通过。 |
| `git diff --check` | 通过；基线运行期间无受控文件空白错误。 |

开发机初次在受限环境中执行 release-check 时无法访问 Docker socket；在获得本机 Docker
访问权限后使用相同工作树重跑，完整门禁通过。该环境差异不属于产品回归。

## 7. 回滚方式

本阶段只新增冻结记录和计划状态。删除本进度文档并把计划表状态改回待开始即可；不涉及
数据库、配置、用户数据或运行时回滚。

## 8. 已知限制

- 基线仍是 `v0.11.0`：未提供验证式注册、邮件 Challenge、Session 轮换、Category 层级或
  Structured Thread。
- 当前 release-check 的版本提示仍写 `v0.11`；A1 会把发布输出改为读取统一版本常量。
- 当前数据目录中 `plugin_data` 为空是运行状态快照，不代表后续插件不能创建数据目录。

## 9. 下一阶段任务

执行 A1 部署 Bootstrap 安全：删除默认管理员密码日志，区分本地开发与生产 Bootstrap
Secret，避免生产系统以已知默认凭据启动，并让发布脚本从统一版本常量读取版本。
