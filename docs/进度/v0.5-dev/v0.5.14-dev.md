# CampusOS v0.5.14-dev 进度说明

> 日期：2026-07-08
> 状态：已完成

## 1. 本次目标

补充一份面向使用者和后续开发者的风格包切换帮助文档，集中说明 `personal-space` 与 `homepage-customizer` 当前支持的切换方式、操作入口、API、源码目录用法和安全筛查边界。

## 2. 完成内容

| 项目 | 说明 |
| --- | --- |
| 个人主页切换方式 | 说明内置 JSON 风格包、HTML 风格片段、zip 拓展风格包、源码目录拓展风格包、回滚和恢复默认 |
| 首页切换方式 | 说明 `homepage-customizer` 结构化配置、首页 zip 拓展风格包和首页源码目录拓展风格包 |
| API 对照 | 汇总用户侧 `/spaces/me/styles/*` 和管理侧 `/home/style-packs/*` 相关接口 |
| 目录说明 | 标明 `data/plugin_data/personal-space/style-packs/` 与 `data/plugin_data/homepage-customizer/style-packs/` 的用途 |
| 安全边界 | 汇总 target、路径、文件大小、HTML 和 CSS 筛查规则，以及当前不开放任意 JavaScript 的边界 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `docs/help/系统设计相关/风格包切换方式说明.md` | 新增风格包切换方式集中说明 |
| `docs/进度/v0.5-dev/v0.5.14-dev.md` | 新增本次文档任务进度记录 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `git diff --check` | 通过 |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过 |

## 5. 后续任务

1. 首页拓展风格包源码目录应用目前仍需要手动输入目录名，后续可在管理端增加目录扫描列表。
2. 后续若新增风格包市场、审核流、版本索引或首页回滚列表，需要同步更新本说明文档。
