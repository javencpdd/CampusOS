# CampusOS v0.5.18-dev 进度说明

> 日期：2026-07-08
> 状态：已完成

## 1. 本次目标

在管理端新增“说明文档”入口，帮助刚接触 CampusOS 的开发者快速理解项目启动、目录边界、插件编写、插件管理和常见排查流程。说明文字要求通俗、详细、简洁，重点面向插件开发和插件运维。

## 2. 完成内容

| 项目 | 说明 |
| --- | --- |
| 管理端说明文档 | 新增 `/docs` 页面，覆盖新开发者上手顺序、关键目录、插件概念、插件编写流程、管理端操作流程、权限/配置/数据边界、常用命令和常见问题 |
| 管理端入口 | 在侧边栏新增“说明文档”，并在仪表盘快捷操作中增加入口 |
| 插件教学内容 | 用通俗语言解释 `data/plugins`、`data/plugin_data`、`plugin.yaml`、builtin/wasm/grpc、config schema、权限、导入、启用、配置、日志和卸载 |
| 文档状态同步 | README、v0.5 第二版计划书和项目上手快照同步到 `v0.5.18-dev` |
| 构建产物 | 因 `admin/dist` 已被仓库跟踪，本次同步刷新管理端构建产物 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `admin/src/views/DeveloperDocsView.vue` | 新增管理端说明文档页面 |
| `admin/src/router/index.ts` | 注册 `/docs` 路由和 `/admin/docs` 兼容重定向 |
| `admin/src/components/AdminLayout.vue` | 侧边栏和面包屑标题增加说明文档入口 |
| `admin/src/views/DashboardView.vue` | 快捷操作增加说明文档按钮 |
| `admin/dist/` | 刷新管理端构建产物 |
| `README.md` | 同步当前进度和管理端说明文档能力 |
| `docs/项目计划书v0/项目计划v0.5/01-v0.5版本计划书第二版.md` | 同步当前基线和后续任务顺序 |
| `skills/campusos-project-onboarding/references/current-project-status.md` | 同步项目上手快照 |
| `docs/进度/v0.5-dev/v0.5.18-dev.md` | 新增本次进度说明 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `cd admin && pnpm build` | 通过；Vite 仍提示既有 chunk size warning |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过 |
| `git diff --check` | 通过 |
| `python3 skills/campusos-readme-update/scripts/check_readme_links.py README.md` | 通过 |

## 5. 后续任务

1. 后续可把管理端说明文档中的命令和插件模板抽成可下载模板或一键复制片段。
2. 风格包运营化二期仍建议继续推进源码目录扫描、可选择列表、首页风格快照和回滚。
3. 插件包治理二期可继续补权限风险分级、安装审计和版本回滚说明。
