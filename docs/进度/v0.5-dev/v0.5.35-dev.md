# CampusOS v0.5.35-dev 进度说明

> 日期：2026-07-10
> 状态：已完成

## 1. 本次目标

优化 `campusos-readme-update` Skill，使根 README 只保留首次访问需要的核心信息，并能把次要内容安全地迁入 `docs/help`、`docs/skills`、`docs/api`、`docs/architecture`、进度或计划目录；被迁移文档必须能从文档门户访问。

## 2. 完成内容

| 项目 | 说明 |
| --- | --- |
| Skill 工作流 | 从“直接更新 README”改为“核实来源、分类内容、先更新目标文档、更新 docs 门户、再精简 README、最后验证”。 |
| README 核心边界 | 明确只保留项目定位、当前状态、最短启动、主要地址、简要仓库结构、核心验证、文档入口和 License。 |
| 文档路由 | 新增 `documentation-routing.md`，定义 help、skills、api、architecture、progress、plan、release 和 Todo 的职责。 |
| 结构审查器 | 新增 `audit_readme_structure.py`，检查 README 大小/章节/详细标题、docs 门户双向链接、分类入口和指定文档可达性。 |
| 链接检查器 | `check_readme_links.py` 支持多个 Markdown 文件、仓库根目录约束和 docs 相对链接，同时保留 `... README.md` 旧调用方式。 |
| Skills 文档 | 新增 `docs/skills/` 索引，将 README Skill 说明迁为 canonical 文档，原 `docs/help/skills相关/` 路径保留跳转。 |
| README 实际收口 | 从根 README 移除 pgAdmin、默认账号、贡献脚本和次要专题直链，详细内容继续由现有开发、账号、数据库和专题文档维护。 |
| Skill 安装 | 仓库版本验证完成后同步到 `/home/jack/.codex/skills/campusos-readme-update/`，供新会话调用。 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `skills/campusos-readme-update/SKILL.md` | 重写触发说明、核心 README 契约、文档迁移顺序、验证和防漂移规则。 |
| `skills/campusos-readme-update/references/*.md` | 更新检查清单并新增内容路由规范。 |
| `skills/campusos-readme-update/scripts/*.py` | 新增结构/可达性检查，增强多文件链接检查。 |
| `skills/campusos-readme-update/agents/openai.yaml` | 同步 UI 说明和默认调用提示。 |
| `README.md`、`docs/README.md` | 实际应用新规则并增加 Skills 文档入口。 |
| `docs/skills/` | 新增 Skills 索引和 README Skill canonical 使用说明。 |
| `docs/help/skills相关/CampusOS-README更新Skill使用说明.md` | 改为兼容历史链接的迁移说明。 |
| v5/v6 计划、API/架构基线和项目上手快照 | 同步 `v0.5.35-dev` 当前文档基线，并让上手快照单独列出 `docs/skills/`。 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `python3 skills/campusos-readme-update/scripts/audit_readme_structure.py --root . --require-doc docs/skills/CampusOS-README更新Skill使用说明.md` | 通过；README 为 70 行、6 个二级章节、10 个直接 docs 链接，指定说明可从门户访问。 |
| `python3 skills/campusos-readme-update/scripts/check_readme_links.py --root . README.md docs/README.md docs/skills/README.md docs/skills/CampusOS-README更新Skill使用说明.md` | 通过。 |
| `python3 skills/campusos-readme-update/scripts/check_readme_links.py README.md` | 通过；确认旧调用方式兼容。 |
| `python3 /home/jack/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/campusos-readme-update` | 通过。 |
| `python3 /home/jack/.codex/skills/.system/skill-creator/scripts/quick_validate.py /home/jack/.codex/skills/campusos-readme-update` | 通过；用户级同步副本可被 Skill 校验器识别。 |
| `bash -n skills/campusos-project-onboarding/scripts/context_snapshot.sh` | 通过；快照可列出新的 `docs/skills/` 文档。 |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过。 |
| `git diff --check` | 通过。 |

## 5. 后续任务

1. 后续新增 Skill 使用说明直接进入 `docs/skills/`；现有三个 legacy Skill 文档可按需迁移，并保留旧路径跳转。
2. v6 的 API 契约工作可继续扩展文档检查器，让 `docs/api` 的 OpenAPI 与实际路由也进入统一漂移检查。
