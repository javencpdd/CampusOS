# CampusOS v0.5.21-dev 进度说明

> 日期：2026-07-08
> 状态：已完成

## 1. 本次目标

把 v0.5 的关键人工回归步骤沉淀为可重复执行的 smoke 脚本，并补充使用说明，作为 v0.5 收口验收的基础工具。

## 2. 完成内容

| 项目 | 说明 |
| --- | --- |
| Smoke 脚本 | 新增 `scripts/smoke/v05_smoke.py`，只使用 Python 标准库，便于本地和 CI 复用 |
| 只读检查 | 覆盖健康检查、版块、帖子、首页配置、富文本插件状态、管理员登录、插件、集成中心、MCP-like、Message 和平台日志来源 |
| 写入检查 | `--write` 模式覆盖注册、登录、普通发帖、回帖楼层、富文本草稿、富文本预览和发布 |
| 参数化 | 支持 API 地址、管理员账号、超时时间和 smoke 邮箱前缀配置 |
| 帮助文档 | 新增 `docs/help/系统设计相关/v0.5回归Smoke测试说明.md` |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `scripts/smoke/v05_smoke.py` | v0.5 smoke runner |
| `docs/help/系统设计相关/v0.5回归Smoke测试说明.md` | smoke 脚本使用说明 |
| `README.md` | 增加 smoke 命令和文档入口 |
| `docs/项目计划书v0/项目计划v0.5/01-v0.5版本计划书第二版.md` | 同步 v0.5.21-dev 完成记录 |
| `skills/campusos-project-onboarding/references/current-project-status.md` | 同步 smoke 脚本位置和命令 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `python3 scripts/smoke/v05_smoke.py --help` | 通过 |
| `python3 scripts/smoke/v05_smoke.py` | 通过 |
| `python3 scripts/smoke/v05_smoke.py --write` | 通过，会创建 smoke 用户、普通帖子、回复和富文本文章 |
| `GOCACHE=/tmp/campusos-go-cache go test ./internal/plugin ./internal/homepage ./internal/richtext ./internal/community/... ./internal/server -count=1` | 通过 |
| `cd web && pnpm build` | 通过；仍有既有 Rollup pure annotation warning 和 Vite chunk size warning |
| `cd admin && pnpm build` | 通过；仍有既有 Rollup pure annotation warning 和 Vite chunk size warning |

## 5. 后续任务

1. 可以把 smoke 脚本接入 CI 的可选 job，避免每次 PR 都强制依赖本地默认管理员账号。
2. 后续可补充 Webhook 测试投递、插件启停恢复和风格包上传类写入 smoke。
