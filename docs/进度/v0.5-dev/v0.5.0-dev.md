# CampusOS v0.5.0-dev 进度说明

> 日期：2026-07-02
> 状态：已完成
> 范围：v5 稳定化与基线确认
> 提交：按 `campusos-dev-nocommit` 要求，本次不提交 commit

## 1. 本次目标

完成 `docs/项目计划v5/00-v5版本计划书.md` 中的第一个任务包：

```text
v0.5.0-dev：稳定化与基线确认
```

本次目标不是开发新功能，而是确认 v4 已完成能力可以作为 v5 后续开发基线。

## 2. 完成内容

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| v5 进度目录建立 | 通过 | 新建 `docs/进度/v0.5-dev`，后续 v5 进度不再占用 v0.4-dev 编号 |
| Docker 服务状态 | 通过 | PostgreSQL、pgAdmin、Redis、NATS 容器均在运行；PostgreSQL 映射为 `5433->5432` |
| pgAdmin 访问回归 | 通过 | `http://127.0.0.1:5050/login` 返回 `200 OK` |
| 数据库迁移回归 | 通过 | `make migrate-up` 可使用 Docker PostgreSQL；`make migrate-status` 显示 `000001` 到 `000010` 均已应用 |
| 后端健康检查 | 通过 | `GET /api/v1/health` 返回 `200 OK` |
| 管理员登录回归 | 通过 | `admin@campusos.local` / `Admin@123456` 登录返回 `200` |
| 用户注册/登录回归 | 通过 | 新建临时 smoke 用户并成功登录 |
| 发帖回归 | 通过 | 临时 smoke 用户可在默认版块创建帖子 |
| 个人主页内容同步回归 | 通过 | 新帖可通过 `/api/v1/u/:username/contents` 在公开个人主页内容列表中查询到 |
| 风格包应用回归 | 通过 | `examples/space-styles/clean-blog.space-style.json` 可通过 `/api/v1/spaces/me/styles/apply` 应用，返回 `200` |
| 后端测试 | 通过 | `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` 全部通过 |
| web 构建 | 通过 | `cd web && pnpm build` 通过 |
| admin 构建 | 通过 | `cd admin && pnpm build` 通过 |
| Docker Compose 配置 | 通过 | `docker compose config -q` 通过；`docker compose ps` 显示依赖服务状态正常 |

本次 API smoke 使用了临时测试用户和帖子：

| 项目 | 值 |
| --- | --- |
| 用户名 | `v050_smoke_1783007134` |
| 用户 ID | `1783007135032065380` |
| 帖子 ID | `1783007135281755046` |
| 版块 ID | `1000000000000000004` |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `docs/进度/v0.5-dev/v0.5.0-dev.md` | 新增 v0.5.0-dev 稳定化与基线确认记录 |

本次未修改后端、前端或数据库迁移文件。

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `docker compose config -q` | 通过 |
| `docker compose ps` | 通过，PostgreSQL、pgAdmin、Redis、NATS 服务正常 |
| `make migrate-up` | 通过，所有已应用 migration 均跳过 |
| `make migrate-status` | 通过，`schema_migrations` 包含 `000001` 到 `000010` |
| `curl http://127.0.0.1:8080/api/v1/health` | 通过，返回 `200 OK` |
| `curl http://127.0.0.1:5050/login` | 通过，返回 `200 OK` |
| 用户注册、登录、发帖、个人主页内容查询 API smoke | 通过 |
| `POST /api/v1/spaces/me/styles/apply` 应用示例风格包 | 通过，返回 `200` |
| 管理员登录 API smoke | 通过，返回 `200` |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过 |
| `cd web && pnpm build` | 通过，有 Vite chunk size 警告，不影响构建结果 |
| `cd admin && pnpm build` | 通过，有 Vite chunk size 警告，不影响构建结果 |

## 5. 后续任务

| 优先级 | 任务 |
| --- | --- |
| P0 | 开始 `v0.5.1-dev`：管理后台集成中心骨架 |
| P0 | 后续 v5 任务继续写入 `docs/进度/v0.5-dev` |
| P1 | 若需要保持数据库干净，可后续增加 smoke 测试数据清理脚本或单独测试库 |
| P1 | Vite chunk size 警告可在后续前端优化任务中处理，目前不阻塞 v5 基线 |
