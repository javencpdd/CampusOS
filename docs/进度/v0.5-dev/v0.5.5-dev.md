# CampusOS v0.5.5-dev 进度说明

> 日期：2026-07-02
> 状态：已完成

## 1. 本次目标

完成 MCP 只读实验闭环，提供受控只读工具、管理员开关和审计记录。

## 2. 完成内容

| 项目 | 说明 |
| --- | --- |
| 只读工具 | 内置 `health.check`、`categories.list`、`threads.list`、`threads.get` |
| 后台开关 | 新增 `GET/PUT /api/v1/mcp/settings`，可启停 MCP 服务 |
| 调用审计 | 每次工具调用记录 user、tool、arguments、success、error、created_at |
| RBAC 约束 | 所有 MCP 管理接口走现有 JWT + `role:manage` 权限 |
| 后台测试 | 集成中心可查看工具列表并调用只读工具 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `migrations/000011_v05_operational_features.up.sql` | 新增 `mcp_audit_logs` |
| `internal/mcp/mcp.go` | MCP 工具注册、调用、审计和 handler |
| `internal/server/server.go` | 注册 MCP 服务和路由 |
| `admin/src/api/index.ts` | MCP API client |
| `admin/src/views/IntegrationCenterView.vue` | MCP 工具列表和调用 UI |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `GOCACHE=/tmp/campusos-go-cache go test ./...` | 通过 |
| `cd admin && pnpm build` | 通过 |
| `GET /api/v1/mcp/tools` | 返回 `200 success` |
| `POST /api/v1/mcp/tools/health.check/call` | 返回 `200 success` |

## 5. 后续任务

当前是 CampusOS 内部 API 形态的 MCP 只读实验闭环。后续如果要接标准 MCP 协议，可在此工具注册层外再加协议适配器，不建议直接开放写工具。
