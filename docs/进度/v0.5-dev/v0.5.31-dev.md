# CampusOS v0.5.31-dev 进度说明

> 日期：2026-07-10
> 状态：已完成

## 1. 本次目标

审查 v1 到 v5 的计划完成度，明确已完成、部分完成与后置事项；以真实实现边界规划 v6；同时让根目录 README 回归项目入口职责，并把运行、架构和 API 的详细内容收拢到 `docs/`。

## 2. 完成内容

| 项目 | 说明 |
| --- | --- |
| 历史计划审查 | 新增 v1-v5 审查文档，逐项区分已完成基础闭环、部分完成和未完成/后置，避免历史愿景被误读为当前交付。 |
| v6 计划书 | 新增以 API 契约、单节点恢复、插件供应链回滚、标准 MCP 只读服务和一个真实协议适配器为主线的 v6 计划。 |
| 范围控制 | v6 明确不把公网插件市场、多 IM 平台、MCP 写操作、AI 强制审核、任意 JavaScript 风格包和高可用部署一次性纳入。 |
| README 收口 | 根 README 从完整操作手册收敛为当前基线、快速启动、关键命令、目录边界和文档入口。 |
| 文档门户 | 新增 `docs/README.md`，按运行、插件、架构、API、计划和进度提供入口。 |
| 详细文档 | 新增开发/验证/贡献指南、当前架构概览和 API 索引；保留旧帮助文档作为细节来源。 |
| v5 同步 | 第二版 v5 计划书补充 v0.5.30 学期 JSON 隔离和 v0.5.31 文档收口，并修正课表存储路径表述。 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `README.md` | 只保留核心状态、快速启动、关键命令与文档导航。 |
| `docs/README.md` | 新增文档门户。 |
| `docs/help/系统设计相关/开发运行与验证指南.md` | 集中启动、验证、`STOP_EXISTING=true make dev-all`、`git_commit.sh` 与 `git_pr.sh` 使用说明。 |
| `docs/architecture/当前架构概览.md` | 记录当前模块、插件和本地数据职责边界。 |
| `docs/api/API索引.md` | 记录当前 API 组、课表 API 与 OpenAPI 契约缺口。 |
| `docs/项目计划v6/00-v1-v5计划审查.md` | 新增历史计划完成度审查。 |
| `docs/项目计划v6/01-v6版本计划书.md` | 新增 v6 计划书。 |
| `docs/项目计划v5/01-v5版本计划书第二版.md` | 同步 v0.5.30、v0.5.31 与课表当前存储布局。 |
| `skills/campusos-project-onboarding/references/current-project-status.md` | 更新文档入口和 v0.5.31 基线。 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过。 |
| `cd web && pnpm build` | 通过。Vite 仅输出既有依赖 Pure 注释和大 chunk 警告。 |
| `python3 skills/campusos-readme-update/scripts/check_readme_links.py README.md` | 通过。 |
| `git diff --check` | 通过。 |

## 5. 后续任务

1. 按 v6 的 API 契约工作包，把当前路由和模型沉淀为可由 CI 校验的完整 OpenAPI 规范。
2. 在开始标准 MCP 或真实消息适配器前，先完成单节点恢复演练和用户级插件版本快照/回滚设计。
3. 浏览器级 E2E 仍未形成发布门槛；可先覆盖登录、发帖、个人空间、课表、插件管理与恢复后的核心访问路径。
