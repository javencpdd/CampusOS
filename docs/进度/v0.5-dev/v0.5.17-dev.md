# CampusOS v0.5.17-dev 进度说明

> 日期：2026-07-08
> 状态：已完成

## 1. 本次目标

把 `personal-space` 和 `homepage-customizer` 的页面拓展风格包从最小 HTML/CSS zip 收敛为标准源码包目录，并确保风格包仍保存在 `data/plugin_data/<plugin>/style-packs/<pack>/`，而不是 `data/plugins` 插件实现目录。

## 2. 完成内容

| 项目 | 说明 |
| --- | --- |
| 标准目录落地 | 源码包标准化为 `style.yaml`、`README.md`、`preview.png`、`templates/page.html`、`templates/card.html`、`assets/`、`styles/theme.css`、`config.schema.json` |
| manifest 扩展 | `page-style-pack.v1` 支持 `templates`、`preview_image`、`config_schema` 和 `assets` 字段 |
| 安全筛查增强 | 后端会检查所有 `.html`、`.css` 文件，校验图片资源路径，并解析根目录 `config.schema.json` |
| 内置示例更新 | 个人主页源码风格包调整为 `data/plugin_data/personal-space/style-packs/clean-blog/`，首页示例为 `data/plugin_data/homepage-customizer/style-packs/campus-hero/` |
| 示例包生成 | `BuildExample` 生成的 zip 示例同步包含标准目录文件和 config schema |
| 文档同步 | 更新 README、风格包切换说明、个人主页风格包说明、插件 README、插件保存位置说明、v0.5 第二版计划书和项目上手快照 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `internal/stylepack/stylepack.go` | 扩展 manifest、源码包示例生成和全文件筛查逻辑 |
| `internal/stylepack/stylepack_test.go` | 增加标准目录、模板 HTML 和 config schema 校验测试 |
| `data/plugin_data/personal-space/style-packs/clean-blog/` | 标准化个人主页源码风格包 |
| `data/plugin_data/homepage-customizer/style-packs/campus-hero/` | 标准化首页源码风格包 |
| `docs/help/系统设计相关/风格包切换方式说明.md` | 更新风格包切换方式和目录标准 |
| `docs/help/插件相关/个人主页风格包说明.md` | 更新 `page-style-pack.v1` 标准字段和筛查规则 |
| `docs/项目计划v0.5/01-v0.5版本计划书第二版.md` | 同步 v0.5.17 当前基线和后续任务顺序 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `GOCACHE=/tmp/campusos-go-cache go test ./internal/stylepack ./internal/space ./internal/homepage ./internal/server -count=1` | 通过 |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过 |
| `git diff --check` | 通过 |
| `python3 skills/campusos-readme-update/scripts/check_readme_links.py README.md` | 通过 |

## 5. 后续任务

1. 增加风格包目录扫描接口，列出 `data/plugin_data/<plugin>/style-packs/` 中通过或失败的源码包。
2. 在管理端和用户端提供源码包选择列表，减少手动输入目录名。
3. 继续评估 `data/style_packages/<target>/` 作为后续跨插件共享风格仓库的必要性。
