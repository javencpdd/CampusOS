# CampusOS v0.7.8-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A5b RichText、Schedule 切换
>
> 状态：已验收

## 1. 阶段目标

去除两功能对 Personal Space 内部实现的依赖。

## 2. 实际完成内容

RichText 图片和 Schedule 学期 JSON 改用 User Storage Core 的路径、布局和根目录规范化能力。

## 3. 主要修改目录和文件

`internal/richtext/assets.go`、`internal/schedule/service.go`

## 4. 架构变化

本阶段按 v7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

原 URL、文件位置、配额配置和 legacy migration 语义保持。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

恢复旧 helper import 即可；文件无迁移。

## 8. 已知限制

Community 旧 Repository 构造依赖仍在 allowlist。

## 9. 下一阶段任务

A5c 固化实现所有权。
