# CampusOS v0.7.10-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A6 Built-in Feature Registry
>
> 状态：已验收

## 1. 阶段目标

统一管理四个内置功能及启停模式。

## 2. 实际完成内容

新增 Feature Registry；注册 Personal Space、RichText、Schedule 为 restart，Appearance 为 hot-gated；Core always-on 不可关闭。

## 3. 主要修改目录和文件

`internal/platform/feature`、`internal/server/platform_modules.go`

## 4. 架构变化

本阶段按 v7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

Legacy Plugin running 状态作为兼容源；静态 Gin 路由不动态增删，停用只由 Service/Gate 拒绝。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

切回 Manager 状态读取；不删除 Feature 数据。

## 8. 已知限制

Admin 尚未新增独立 Feature API，继续显示 legacy 状态。

## 9. 下一阶段任务

A7 建立资源包合同。
