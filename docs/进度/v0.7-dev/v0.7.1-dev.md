# CampusOS v0.7.1-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A0.5 当前系统基线冻结
>
> 状态：已验收

## 1. 阶段目标

在继续修改 Module Kernel 和 Bootstrap 前冻结可重复比较的 `v0.6.26-dev` 运行、数据和合同基线。

## 2. 实际完成内容

- 保存 HTTP 路由、OpenAPI、权限合同的数量与 checksum。
- 记录 18 个 migration、30 张表、schema contract 和数据体检结果。
- 记录 9 个 Plugin Manifest、三种 Runtime 和当前 UI Runtime Manifest 摘要。
- 记录现有 `data/` 持久化根目录和 Legacy Resource Source。
- 完整执行发布门禁、恢复演练、浏览器工作流和 PC/移动端风格包流程。

## 3. 主要修改目录和文件

- `docs/architecture/v0.7-current-system-baseline.json`
- `docs/architecture/v0.7当前系统基线.md`
- `docs/进度/v0.7-dev/v0.7.1-dev.md`

## 4. 架构变化

无运行时架构变化。本阶段建立后续阶段的兼容比较依据。

## 5. 兼容和迁移处理

没有修改 API、migration、Manifest 和数据目录。快照明确区分稳定合同与运行时 revision、时间戳、用户数据值等非稳定内容。

## 6. 测试与构建结果

执行 `RUN_RESTORE_DRILL=true RUN_BROWSER_SMOKE=true GOCACHE=/tmp/campusos-go-cache make release-check`，最终通过：

- contracts、文档链接、全量 Go 测试、database/schema/architecture check 通过。
- Web lint/Prettier/build、Admin build、docs-site build、TypeScript SDK build 通过。
- builtin、gRPC、Wasm 三模板通过。
- 隔离恢复、浏览器业务流程、PC/移动端风格包流程通过。
- `git diff --check` 通过。

## 7. 回滚方式

本阶段无数据修改。基线文件可独立删除，但后续阶段在重新捕获等价快照前不得移除比较依据。

## 8. 已知限制

Web/Admin 仍有 Vite 大 chunk 告警；当前 UI Runtime 可见插件数受启停配置影响，不作为固定合同。

## 9. 下一阶段任务

执行 A1：将 Module Kernel 改为基础 Module 加可选 Starter、Stopper、HealthReporter，并补齐无生命周期能力模块的测试。
