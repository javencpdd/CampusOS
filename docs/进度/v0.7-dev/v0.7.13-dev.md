# CampusOS v0.7.13-dev 进度说明

> 日期：2026-07-12
>
> 阶段：B1a Plugin Catalog 与 Runtime Registry
>
> 状态：已验收

## 1. 阶段目标

从 Manager 拆出目录分类和 Runtime 注册职责。

## 2. 实际完成内容

新增实际拥有插件索引的 PluginCatalog 与拥有 Runtime map 的 RuntimeRegistry；Legacy builtin 映射到 Core/Feature，外部目录只通过 `ListExternal` 暴露 gRPC/Wasm 等扩展。

## 3. 主要修改目录和文件

`internal/plugin/platform_services.go`、`manager.go`

## 4. 架构变化

本阶段按 v7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

Manager Facade 及 API 不变，旧 builtin 仍可兼容加载。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

Manager 可恢复内部 runtime map。

## 8. 已知限制

legacy builtin 仍由兼容 Manager 持有运行记录。

## 9. 下一阶段任务

B1b 拆 Lifecycle 与 Config。
