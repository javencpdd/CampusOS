# CampusOS v0.7.14-dev 进度说明

> 日期：2026-07-12
>
> 阶段：B1b Plugin Lifecycle 与 Config
>
> 状态：已验收

## 1. 阶段目标

提供独立生命周期和配置委托服务。

## 2. 实际完成内容

新增 LifecycleService、ConfigService；Manager 的公开生命周期和配置方法已委托到子服务，旧方法签名保持不变。

## 3. 主要修改目录和文件

`internal/plugin/platform_services.go`、`manager.go`

## 4. 架构变化

本阶段按 v0.7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

现有 Start/Stop/RequestEnable/Disable/UpdateConfig 行为不变。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

调用方继续使用 Manager 原方法即可。

## 8. 已知限制

内部实现仍逐步迁移，Facade 暂时双入口。

## 9. 下一阶段任务

B1c 拆 UI、Event、Audit。
