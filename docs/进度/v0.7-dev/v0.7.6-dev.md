# CampusOS v0.7.6-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A4 Moderation Core
>
> 状态：已验收

## 1. 阶段目标

确保治理权限、板块作用域、审计和完整性不受插件开关影响。

## 2. 实际完成内容

Server 对 Moderation Core 始终启用；`allow_pin`、`allow_lock`、`allow_delete_post` 继续从 legacy 配置热读取，仅控制动作。

## 3. 主要修改目录和文件

`internal/server/application.go`、`internal/moderation`

## 4. 架构变化

本阶段按 v0.7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

旧 category-moderation 配置和管理 UI 继续可用；关闭 legacy 插件不再卸载安全策略。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

可恢复旧 enabled checker，但不得关闭 RBAC、作用域和审计。

## 8. 已知限制

旧 manifest 仍用于兼容配置入口。

## 9. 下一阶段任务

A5a 建立 User Storage Adapter。
