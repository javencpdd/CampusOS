# CampusOS v0.7.3-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A2a 基础设施 Bootstrap 拆分
>
> 状态：已验收

## 1. 阶段目标

将进程基础设施从业务装配中分离。

## 2. 实际完成内容

新增 `internal/server/bootstrap.go`，统一装配 Module Registry、EventBus、Plugin Platform、Cache、Metrics 和逆序停止；`server.go` 仅保留稳定入口。

## 3. 主要修改目录和文件

`internal/server/server.go`、`bootstrap.go`、`application.go`

## 4. 架构变化

本阶段按 v7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

基础设施启动失败直接返回，停止使用 10 秒超时；不改 API 和数据。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

切回原单文件 Run 装配；无数据回滚。

## 8. 已知限制

业务 composition root 仍保留在 `application.go`。

## 9. 下一阶段任务

A2b 拆分业务装配边界。
