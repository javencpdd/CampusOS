# CampusOS v0.7.7-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A5a User Storage Port 与旧 Adapter
>
> 状态：已验收

## 1. 阶段目标

独立安全路径、目录和存储 Port。

## 2. 实际完成内容

新增 `internal/core/storage` 的 Port、Quota、SafePath、AssetStore、Provider 与 LocalAdapter；保持 `data/personal-space` 布局。

## 3. 主要修改目录和文件

`internal/core/storage`

## 4. 架构变化

本阶段按 v7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

旧根目录自动规范化，拒绝 `..` 和跨用户路径。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

调用方可临时切回 Space helper；不移动数据。

## 8. 已知限制

Provider 目前只有本地合同。

## 9. 下一阶段任务

A5b 切换 RichText 和 Schedule。
