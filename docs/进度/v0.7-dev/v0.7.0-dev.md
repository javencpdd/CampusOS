# CampusOS v0.7.0-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A0 计划、模块分类和数据所有权
>
> 状态：已验收

## 1. 阶段目标

审查并收窄 v0.7 范围，将大规模架构方向拆成可独立实现、回归和回滚的小阶段。

## 2. 实际完成内容

- 明确 Core Module、Built-in Feature、External Plugin、Resource Package 四类合同。
- 将实施顺序修订为 A0、A0.5、A1、A2a、A2b、A3、A4、A5a/A5b/A5c、A6、A7、A8、B1a/B1b/B1c、B2、Final。
- 将 Agent 范围收窄为接口、权限、安全策略和扩展合同，移除本轮完整 Agent 产品交付。
- 增加 Feature 的 `always-on`、`restart`、`hot-gated` 启停合同。
- 将实现状态与验收状态分离，并定义统一 DoD、兼容和回滚要求。

## 3. 主要修改目录和文件

- `docs/项目计划v7/00-v7版本计划书.md`
- `scripts/check-doc-links.py`

## 4. 架构变化

本阶段只修订合同和边界，不改变运行时代码、数据库、API 或持久化目录。

## 5. 兼容和迁移处理

旧 `runtime: builtin`、API、migration 和数据目录继续保留。计划明确后续每个阶段先接入 Adapter，再迁移所有权。

## 6. 测试与构建结果

- `make docs-links`：已纳入后续完整 release-check 并通过。
- `git diff --check`：已纳入后续完整 release-check 并通过。

## 7. 回滚方式

本阶段只有文档修订；回滚只需恢复上一版计划，不涉及代码和数据。

## 8. 已知限制

计划状态不代表代码已完成。A1 和 A2a 仅有部分实现，必须重新按新合同验收。

## 9. 下一阶段任务

执行 A0.5，冻结代码修改前的测试、构建、HTTP、数据库、Plugin/Runtime 和数据目录基线。
