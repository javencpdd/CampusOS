# CampusOS v0.7.5-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A3 Ports、依赖规则和边界检查
>
> 状态：已验收

## 1. 阶段目标

建立公开 Port 并阻止新增跨模块具体实现依赖。

## 2. 实际完成内容

新增 Identity、Community、Plugin、User Storage Port；新增 allowlist 架构检查器、Makefile、release-check 和 CI 门禁。

## 3. 主要修改目录和文件

`internal/*/port`、`internal/core/storage`、`scripts/check-architecture-boundaries.py`、`config/architecture-boundary-allowlist.json`

## 4. 架构变化

本阶段按 v7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

8 个历史例外有原因说明，其中两个仅属于 Server composition root；RichText/Schedule 对 Space 内部实现的生产依赖归零。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

移除检查命令可恢复旧门禁；运行时不受影响。

## 8. 已知限制

历史 allowlist 尚未全部清零。

## 9. 下一阶段任务

A4 将 Moderation 安全能力固定为 Core。
