# CampusOS v0.7.4-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A2b 业务模块装配拆分
>
> 状态：已验收

## 1. 阶段目标

移出 `server.go` 中的业务 Repository、Service、Handler 和路由细节。

## 2. 实际完成内容

将兼容业务 composition root 与静态路由迁到 `internal/server/application.go`；合同生成器同步读取新入口，148 条路由内容保持不变。

## 3. 主要修改目录和文件

`internal/server/application.go`、`internal/projectaudit`、`cmd/campusos-contracts`

## 4. 架构变化

本阶段按 v7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

路由 JSON、OpenAPI 和权限 checksum 保持冻结值；授权矩阵只更新生成来源说明。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

恢复旧文件布局并重新生成合同；不改数据库。

## 8. 已知限制

业务装配仍是兼容 composition root，后续新增模块必须使用 Port。

## 9. 下一阶段任务

A3 建立 Port 和自动边界检查。
