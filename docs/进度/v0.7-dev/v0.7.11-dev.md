# CampusOS v0.7.11-dev 进度说明

> 日期：2026-07-12
>
> 阶段：A7 Resource Package 与 Legacy Source
>
> 状态：已验收

## 1. 阶段目标

建立无业务生命周期的统一资源仓库。

## 2. 实际完成内容

新增 `campusos.resource/v1` Manifest、FileRepository、校验器、新目录和 Legacy Source 读取。

## 3. 主要修改目录和文件

`internal/platform/resource`、`data/resources`

## 4. 架构变化

本阶段按 v0.7 四类能力和渐进适配原则收紧对应边界，不新增微服务或全局 Service Locator。

## 5. 兼容和迁移处理

旧 `data/plugin_data/*/style-packs` 保留；禁止 runtime、migration 和启动脚本。

## 6. 测试与构建结果

定向 Go 测试与 `make architecture-check` 通过；全量 Go、三前端、SDK、数据库、恢复和浏览器证据在 Final 文档统一记录。

## 7. 回滚方式

禁用新 Repository 后继续读 Legacy Source。

## 8. 已知限制

尚未迁移现有资源文件。

## 9. 下一阶段任务

A8 建立 Appearance Facade。
