# CampusOS v0.6.18-dev 进度说明

> 日期：2026-07-12  
> 状态：已完成（WP8）

## 1. 目标
在 v1 安全筛查基础上增加可设计完整视口的 `campusos.app-style-pack.v2`。

## 2. 实际完成与核心设计
v2 增加 Layout Recipe、全屏背景资源、内容宽度、间距、Header/scroll 模式、侧栏、受控动画和 Surface Override；仍执行路径、HTML、CSS scope、资源和 sandbox-worker 检查。ThemeRoot 映射 token/layout，失败回退旧主题或 Core 默认。

## 3. 修改文件
`internal/stylepack/stylepack.go`、`web/src/campus-ui/ThemeRoot.vue`、`data/plugin_data/web-theme/style-packs/aurora-campus/`。

## 4. API / 数据库 / 兼容
`page-style-pack.v1` 和 `target:web` 保留；v2 只允许 target:web。风格包不能替换 PageOutlet/Safety 或改变 Action。

## 5. 测试
`go test ./internal/stylepack ./internal/webtheme -count=1`：通过；真实 Aurora Campus 包也由测试加载校验。Web build：通过。

## 6. 已知限制与后续
动画仅使用 Core preset 与既有隔离 Canvas；不开放任意同源 JS。示例背景 PNG 较大，发布前可增加标准图片压缩流水线。
