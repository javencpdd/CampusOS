# CampusOS v0.6.14-dev 进度说明

> 日期：2026-07-12  
> 状态：已完成（WP4）

## 1. 目标
建立 `campusos.ui/v1` 的 Route、Navigation、Slot、Surface 和 Action 合同。

## 2. 实际完成与核心设计
Manifest/Go SDK/Web 类型同步增加 UIContribution。安装校验拒绝重复或非法 ID、危险 route/action 路径、未知引用、缺少默认 renderer、未知 HTTP Method 和非法 host slot。Action 固定 method/path/permission/confirm/audit/body，风格包无权修改。

## 3. 修改文件
`internal/plugin/ui_contract.go`、`manifest.go`、`sdk/go/types.go`、`web/src/campus-ui/contracts.ts`。

## 4. API / 数据库 / 兼容
UI 字段为可选加法字段；没有 UI 的旧插件继续运行。

## 5. 测试
`go test ./internal/plugin -count=1`：通过；未知 Action 和默认值有定向测试。

## 6. 已知限制与后续
合同当前为 v1；兼容 resolver 对未知 contract 使用标准不兼容 Surface，不做静默降级。
