# CampusOS v0.6.19-dev 进度说明

> 日期：2026-07-12  
> 状态：已完成（WP9）

## 1. 目标
用非空壳示例证明安装、动态 UI、Action、Gateway、Runtime 和审计完整链路。

## 2. 实际完成与核心设计
新增 `campus-welcome` builtin 示例，Manifest 提供 route/navigation/schema surface/action；服务端 handler 返回由 Core 注入的 caller 与 trace。页面无需重建 Web 即可随 Runtime Manifest 出现，Action 真实调用 Gateway。

## 3. 修改文件
`data/plugins/campus-welcome/plugin.yaml`、`README.md`、`internal/server/server.go`、`internal/plugin/runtime_http_test.go`。

## 4. API / 数据库 / 兼容
示例使用 `/api/v1/extensions/campus-welcome/welcome`；审计写现有插件日志，不创建独立业务表。

## 5. 测试
Gateway 测试证明客户端伪造 user ID 不会进入可信 caller；Go 定向测试和 Web build 通过。

## 6. 已知限制与后续
示例用于合同教学，不替代真实业务插件；其后端随 Core 编译，因此 activation mode 为 restart。
