# CampusOS v0.6.20-dev 进度说明

> 日期：2026-07-12  
> 状态：已完成（WP10）

## 1. 目标
让现有插件渐进进入新前端运行时，不一次删除全部旧实现。

## 2. 实际完成与核心设计
personal-schedule、personal-space、web-theme 增加 UI Contract，并通过 `core.schedule`、`core.personal-space`、`core.appearance` trusted module 承载成熟页面；对应静态路由移除，公共个人主页、帖子、富文本和版主流程保持旧路由。

## 3. 修改文件
三个 `data/plugins/*/plugin.yaml`、`web/src/router/index.ts`、`PluginSurfacePage.vue`。

## 4. API / 数据库 / 兼容
原 API、数据目录和权限不变。未迁移插件继续没有 UI 声明或使用原页面；禁用已迁移插件会热卸载其设置入口，但不删数据。

## 5. 测试
所有插件 manifest 解析、Go 全量测试和 Web build：通过。

## 6. 已知限制与后续
富文本编辑器和版主 thread widget 尚未拆为细粒度动态 Surface，仍由现有页面集成；这是渐进迁移而非删除旧实现。
