# CampusOS v0.6.10-dev 进度说明

> 日期：2026-07-12  
> 状态：已完成（WP0）

## 1. 本次目标

在代码变更前审查 v0.6 历史计划、当前进度、插件 Runtime、Manifest、前端外壳、路由、风格包、Admin、SDK、migration 和发布门禁，建立插件前端运行时、生命周期与全屏风格包升级的计划和兼容基线。

## 2. 实际完成内容

- 确认 `01-v0.6版本计划书.md` 已是第二版，保留历史文件并新增第三版。
- 将本主线标记为 `P0-Highest / 当前最高优先级主线`，不改写 `v0.6.0-dev` 至 `v0.6.9-dev` 的已完成状态。
- 根据当前最高进度从 `v0.6.10-dev` 起为 WP0-WP11 和最终验收顺序分配编号。
- 明确旧 Manifest、旧 status/API、三类 Runtime、静态路由、`page-style-pack.v1`、`target: web` 和用户数据的兼容策略。
- 更新文档门户，使第二版历史计划和第三版当前计划均可发现。

## 3. 核心设计

Core 继续拥有身份、权限、Gateway、App Shell、UI Contract、兼容和回退；插件拥有业务和默认 UI；风格包只通过 ThemeRoot、Layout Recipe、Token、受控 CSS/资源和 Surface Override 改变完整视口，不改变业务 API 和授权。

## 4. 修改文件

- `docs/项目计划v0.6/02-v0.6版本计划书第三版.md`
- `docs/README.md`
- `docs/进度/v0.6-dev/v0.6.10-dev.md`

## 5. API 或数据库变化

本工作包不修改 API、数据库或运行行为。计划中的首个追加 migration 从 WP1 实施，旧 migration 不重写。

## 6. 兼容处理

- 第二版计划和历史进度保持原样。
- 第三版明确采用加法式 Manifest、API 和 schema 演进。
- 静态页面和 v1 风格包保留为动态运行时及 v2 主题的回退路径。

## 7. 测试命令和结果

- `git diff --check`：通过。
- `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1`：通过。
- `cd web && pnpm build`：通过；保留现有大 chunk 提示。
- `cd admin && pnpm build`：通过；保留现有大 chunk 提示。
- `cd docs-site && pnpm build`：通过。

## 8. 已知限制

WP0 仅建立可实施计划和兼容基线；生命周期、Gateway、动态 UI、SSE、Renderer 和 v2 风格包从 WP1 起依赖顺序实现。

## 9. 后续工作

进入 WP1：扩展 Manifest lifecycle、建立后端/前端/健康三轴状态，追加数据库 migration，并修正 Wasm 更新的原子切换行为。
