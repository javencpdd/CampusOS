# CampusOS v0.6.15-dev 进度说明

> 日期：2026-07-12  
> 状态：已完成（WP5）

## 1. 目标
实现插件前端 Route、Navigation、Slot、Surface、Action 的热注册、更新和完整清理。

## 2. 实际完成与核心设计
Web RuntimeRegistry 先构建并校验候选 snapshot，再替换旧 snapshot；每个 `router.addRoute` 保存 disposer。禁用/卸载时统一清理路由、菜单、插槽、Surface、Action 和缓存；直接访问尚未注册的动态路由会在同步后重新匹配。

## 3. 修改文件
`web/src/campus-ui/runtimeRegistry.ts`、`web/src/stores/uiRuntime.ts`、`PluginSurfacePage.vue`、`web/src/router/index.ts`。

## 4. API / 数据库 / 兼容
保留未迁移静态路由；已迁移插件由 Runtime Manifest 拥有路由。

## 5. 测试
`cd web && pnpm build`：通过，动态页面单独分 chunk。

## 6. 已知限制与后续
当前没有独立前端单元测试框架，注册/清理由浏览器 smoke 和构建验证覆盖。
