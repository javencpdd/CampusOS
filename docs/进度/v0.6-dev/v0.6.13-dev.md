# CampusOS v0.6.13-dev 进度说明

> 日期：2026-07-12  
> 状态：已完成（WP3）

## 1. 目标
将硬编码 `App.vue` 重构为可承载插件和全屏主题的 Campus UI 外壳。

## 2. 实际完成与核心设计
建立 `CampusApp -> ThemeRoot -> AppShell`，包含 Header、Primary/Secondary Navigation、Hero、左右 Sidebar、PageOutlet、Floating Action、Footer 和不可删除 Safety 区。旧页面继续由 PageOutlet 承载，ThemeRoot 保留 `.app-container[data-campusos-web]` 兼容根。

## 3. 修改文件
`web/src/App.vue`、`web/src/campus-ui/CampusApp.vue`、`ThemeRoot.vue`、`AppShell.vue`、Campus UI primitive。

## 4. API / 数据库 / 兼容
无数据库变化；旧 target:web CSS 根继续有效，用户菜单、登录入口和全局健康提示由 Core 保留。

## 5. 测试
`cd web && pnpm build`：通过。

## 6. 已知限制与后续
Core 目前只封装本次 renderer 所需 primitive，后续按真实插件需求扩展语义组件。
