# CampusOS v0.6.6-dev 进度：开发者文档与兼容矩阵

> 日期：2026-07-11
> 状态：已完成

## 已完成

1. 官方文档站增加 current contract、Host API/权限、SDK/CLI、兼容矩阵和恢复运维章节。
2. 仓库帮助文档增加从 Runtime 选择、Manifest、测试、打包到回滚的完整插件教程。
3. API 索引切换到生成的 `openapi-v0.6-current.yaml`、路由 JSON 和授权矩阵。
4. 明确兼容矩阵：CampusOS `0.6.x`、Manifest `campusos.plugin/v1`、Host API `v1`、Go SDK `v0.6`。
5. TypeScript SDK 评估完成：浏览器不持有 Host token；待字段级 schema 稳定后从 Public OpenAPI 生成，不复制 Go Host API。
6. 管理端继续只保留官方文档和 GitHub 入口，详细教程由可独立部署的 `docs-site/` 承载。

## 验证

- `cd docs-site && pnpm build` 通过。
- Chrome headless 对 `http://localhost:3002` 的真实页面 smoke 通过。
- `git diff --check` 和 contract drift check 通过。
