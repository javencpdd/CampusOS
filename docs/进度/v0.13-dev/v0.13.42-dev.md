# v0.13.42-dev：统一错误合同生成器与版本命名同步

> 阶段：`v0.13-dev`（已封版阶段的 CI 兼容维护）
> 日期：2026-08-31
> 更新时间：2026-08-31 15:45（Asia/Shanghai）
> 状态：已完成
> Git：使用 `campusos-dev-nocommit`；未执行 commit 或 push。

## 1. 本次目标

修复 CI `make contracts-check` 中 `docs/api/v13统一错误合同.md` 的生成合同漂移。

## 2. 完成内容

- 根因是项目计划版本命名已统一为 `v0.13`，但 `cmd/campusos-contracts` 的 `errorCatalogMarkdown` 仍生成
  `# CampusOS v13 统一错误合同`；生成文件被更新为 `v0.13` 后，生成器与受管文档不一致。
- 将生成器标题同步为 `# CampusOS v0.13 统一错误合同`，并执行 `--write` 重建受管合同文件。
- 不修改错误码、HTTP 状态、API 路由、兼容字段或错误安全文案。

## 3. 修改文件

| 文件 | 变更 |
| --- | --- |
| `cmd/campusos-contracts/main.go` | 统一错误合同的生成标题使用产品计划命名 `v0.13`。 |
| `docs/api/v13统一错误合同.md` | 由生成器重建，和源代码保持一致。 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `go run ./cmd/campusos-contracts --write` | 通过；重新生成全部受管合同。 |
| `go run ./cmd/campusos-contracts --check` | 通过；10 份受管合同均无漂移，等价于 `make contracts-check` 的实际配方。 |
| `go test ./cmd/campusos-contracts` | 通过（该命令包暂无测试文件）。 |
| `go test ./pkg/apperror ./pkg/response ./pkg/middleware -count=1` | 通过。 |
| `python scripts/check-error-contracts.py` | 通过；错误目录翻译器与原始错误防护一致。 |
| `python scripts/check-doc-links.py` | 通过，511 个本地链接可解析。 |
| `git diff --check`、`python scripts/check-line-endings.py --include-untracked` | 通过。 |

当前 Windows PowerShell/Git Bash 环境未安装 GNU Make，因此未直接执行 `make contracts-check`；其唯一配方
`go run ./cmd/campusos-contracts --check` 已在仓库内 Go 缓存环境成功执行，CI 会在 Linux Runner 上直接复验 Make 目标。

## 5. 后续任务

- 提交后观察 GitHub Actions 的 `contracts-check`；预期不再报告 `v13统一错误合同.md` 漂移。
- 未来修改 `pkg/apperror` 或合同生成器时，先运行 `go run ./cmd/campusos-contracts --write`，再运行 `--check`。
