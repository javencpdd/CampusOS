# CampusOS v0.4.13-dev 进度说明

> 日期：2026-07-02
> 状态：已完成
> 范围：UI 测试 Bug 修复、pgAdmin 连接修复、v4 修复说明文档

## 1. 本次目标

在进入 v5 计划开发前，先处理 `docs/Todo/bug` 中记录的 v4 阶段 UI 测试问题：

| 问题 | 目标 |
| --- | --- |
| 用户主页接口 500 | 修复 `user_space_contents.tags` 写入 NULL 的问题 |
| pgAdmin 无法连接 PostgreSQL | 修复本地 Docker PostgreSQL 与宿主机 5432 端口冲突后的连接路径 |
| admin 快捷入口空白 | 修复 `/admin/*` 路由进入空白页 |
| 插件日志弹窗报错 | 修复插件日志表格接收非数组数据的问题 |

## 2. 完成内容

| 模块 | 完成内容 |
| --- | --- |
| 个人主页内容同步 | `SyncThread` 将 nil tags 归一化为空数组；PG 写入前再次防御 |
| 个人主页历史内容 | 访问个人主页内容列表时继续支持按作者回填已发布帖子 |
| admin 路由 | 仪表盘快捷入口改为命名路由，并增加 `/admin/*` 兼容重定向 |
| 插件日志 | 插件列表和日志列表统一通过 `responseItems` 提取数组，避免 `rows is not iterable` |
| pgAdmin | 本地 `.env` 改用 `POSTGRES_PORT=5433`，重建 postgres/pgadmin 容器，pgAdmin 可解析并连接 `postgres:5432` |
| 文档 | 更新 README、数据库管理指南，新增 v4 UI 测试 Bug 修复说明 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `internal/space/sync.go` | nil tags 归一化、个人主页内容回填逻辑 |
| `internal/space/pg_repository.go` | PG 写入 `user_space_contents` 前防御 nil tags |
| `internal/space/sync_test.go` | 增加历史内容回填和 nil tags 归一化测试 |
| `internal/space/service.go` | 注入 thread repository 用于内容回填 |
| `internal/server/server.go` | 服务启动时为 Space Service 注入 thread repository |
| `admin/src/router/index.ts` | 增加 `/admin/*` 兼容重定向 |
| `admin/src/views/DashboardView.vue` | 快捷入口改为命名路由 |
| `admin/src/views/PluginManageView.vue` | 插件列表和日志列表响应数组归一化 |
| `admin/src/api/index.ts` | 登录请求 401 不再触发全局跳转，保留错误提示 |
| `docker-compose.yml` | pgAdmin 配置补强 |
| `scripts/docker-up.sh` | `make docker-up` 纳入 pgAdmin 并检查端口 |
| `scripts/docker-up.ps1` | Windows 脚本同步 pgAdmin 启动检查 |
| `migrations/000004_seed_admin.up.sql` | 修正默认 admin 密码哈希 |
| `migrations/000010_fix_admin_seed_password.*.sql` | 修复已迁移环境中的默认 admin 密码哈希 |
| `README.md` | 同步端口、默认账号和迁移列表 |
| `docs/help/数据库管理指南.md` | 更新 pgAdmin 连接方式和 5432 冲突处理 |
| `docs/项目计划v4/01-v4-UI测试Bug修复说明.md` | 新增本次处理过程与思路 |

本地 `.env` 也设置了 `POSTGRES_PORT=5433` 和匹配的 `DATABASE_DSN`，但该文件被 `.gitignore` 忽略，不作为仓库提交内容。

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `GOCACHE=/tmp/campusos-go-cache go test ./internal/space -count=1` | 通过 |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过 |
| `cd admin && npm run build` | 通过，仅有 Vite/Rollup 既有 chunk 和注释警告 |
| `docker compose config -q` | 通过 |
| `docker compose up -d --force-recreate postgres pgadmin` | 通过，未删除 volume |
| `docker exec campusos-pgadmin getent hosts postgres` | 通过 |
| `docker exec campusos-pgadmin python3 -c 'import socket; s=socket.create_connection(("postgres",5432),5); print("postgres:5432 reachable"); s.close()'` | 通过 |
| `curl -I --max-time 10 http://127.0.0.1:5050/` | 通过，返回 302 到 pgAdmin 登录页 |
| `PGPASSWORD=campusos_dev psql -h 127.0.0.1 -p 5433 -U campusos -d campusos -c 'SELECT 1;'` | 通过 |
| `make migrate-up && make migrate-status` | 通过，迁移状态到 `000010_fix_admin_seed_password` |

## 5. 后续任务

| 优先级 | 任务 |
| --- | --- |
| P0 | 手动通过 UI 回归：注册/登录/发帖、个人主页内容、风格包应用、admin 快捷入口、插件日志 |
| P1 | 后续后台页面统一使用命名路由跳转 |
| P1 | 前端所有表格接口统一增加数组归一化工具，减少类似 `rows is not iterable` 回归 |
| P1 | v5 前置稳定化中保留 pgAdmin/端口冲突检查项 |
