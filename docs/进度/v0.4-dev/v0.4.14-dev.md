# CampusOS v0.4.14-dev 进度说明

> 日期：2026-07-02
> 状态：已完成
> 范围：PostgreSQL 双实例数据不一致说明文档

## 1. 本次目标

解释以下两种连接方式查询数据不一致的原因，并将说明文档保存到 `docs/help`：

```bash
PGPASSWORD=campusos_dev psql -h 127.0.0.1 -p 5432 -U campusos -d campusos
docker exec -it campusos-postgres psql -U campusos -d campusos
```

## 2. 完成内容

| 项目 | 说明 |
| --- | --- |
| 双实例确认 | 确认宿主机 `127.0.0.1:5432` 是本机 PostgreSQL，Docker `campusos-postgres` 映射到宿主机 `5433` |
| 数据差异说明 | 记录 `5432`、`5433`、`docker exec` 三种连接看到的数据量差异 |
| 使用建议 | 明确以 Docker PostgreSQL 为准时应使用 `5433`，pgAdmin 容器内仍使用 `postgres:5432` |
| 迁移方案 | 增加从宿主机 `5432` 备份并恢复到 Docker `5433` 的示例命令 |
| 主文档入口 | 在 `docs/help/数据库管理指南.md` 中增加该问题的跳转说明 |

## 3. 修改文件

| 文件 | 说明 |
| --- | --- |
| `docs/help/PostgreSQL双实例数据不一致说明.md` | 新增双实例原因、验证结果、正确使用方式、数据迁移方案 |
| `docs/help/数据库管理指南.md` | 增加“宿主机 psql 和 docker exec 查询结果不一致”排查入口 |
| `docs/进度/v0.4-dev/v0.4.14-dev.md` | 记录本次文档任务 |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `git diff --check -- docs/help/PostgreSQL双实例数据不一致说明.md docs/help/数据库管理指南.md` | 通过 |
| `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` | 通过 |

## 5. 后续任务

| 优先级 | 任务 |
| --- | --- |
| P0 | 选择一个数据库作为开发主库，避免宿主机 PostgreSQL 和 Docker PostgreSQL 混用 |
| P1 | 如需保留宿主机 `5432` 的测试数据，先备份再恢复到 Docker `5433` |
