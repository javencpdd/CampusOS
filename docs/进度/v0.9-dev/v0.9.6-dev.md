# CampusOS v0.9.6-dev：A5 五视口回归门禁

> 实现状态：已完成  
> 验收状态：已验收

## 阶段目标

将多端验收变为可执行的浏览器脚本，而非人工口头检查。

## 实际完成内容

- 新增 Playwright Core/Chrome 流程，默认覆盖 `360x800`、`390x844`、`768x1024`、`1440x900`、`1920x1080`。
- 支持 `RESPONSIVE_VIEWPORTS` 分组执行，CI 默认仍执行全量；截图按路由和视口保存。
- `scripts/smoke/browser-smoke.sh` 接入五视口流程。

## 主要修改目录和文件

- `web/tests/responsive-workflow.mjs`
- `scripts/smoke/browser-smoke.sh`

## 架构、兼容与回滚

测试只访问公开页面与登录流程，不修改业务数据。脚本可独立删除，不影响服务运行。

## 测试与构建结果

- 三组真实 Chrome 执行全部通过，共 60 张截图。
- 人工检查了用户插件中心手机图、Admin 插件中心手机图和宽屏图。

## 已知限制与下一阶段

截图基线目前用于人工审查，尚未使用像素差阈值。下一阶段交付 v2 Manifest 与 SDK 合同。
