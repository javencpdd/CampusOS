# CampusOS v0.9.5-dev：A4 性能与懒加载

> 实现状态：已完成  
> 验收状态：已验收

## 阶段目标

降低 Web/Admin 初始包压力，并让组件库按实际使用拆分。

## 实际完成内容

- Web/Admin 使用 `unplugin-vue-components` 和 Element Plus Resolver 自动按需导入组件。
- 移除全量 `app.use(ElementPlus)`，保留现有路由级异步加载；Vite 将框架代码与组件 chunk 分离。
- 更新前端预算检查器。

## 主要修改目录和文件

- `web/vite.config.ts`、`web/src/main.ts`
- `admin/vite.config.ts`、`admin/src/main.ts`
- `scripts/check-frontend-bundles.py`

## 架构、兼容与回滚

不改变鉴权和 Feature gate 时序；自动导入失败会在构建期暴露。回滚可恢复全量注册方式。

## 测试与构建结果

- `make frontend-budget` 通过：Web JS 431637/500000 bytes，Admin JS 9826/250000 bytes；CSS 均低于 400000 bytes。
- Web/Admin production build 通过。

## 已知限制与下一阶段

预算当前检查入口文件，不承诺网络级性能指标。下一阶段将其接入固定视口回归。
