# CampusOS v0.9.2-dev：A1 前端测试基础

> 实现状态：已完成  
> 验收状态：已验收

## 阶段目标

为 Web 与 Admin 建立可重复的组件测试命令和网络 Mock 基础。

## 实际完成内容

- Web/Admin 增加 Vite 5 兼容的 Vitest、jsdom、Vue Test Utils 配置与 `test:component` 命令。
- 为两个插件中心增加加载后的关键内容渲染测试，网络调用均使用本地 Mock。

## 主要修改目录和文件

- `web/vitest.config.ts`、`web/tests/plugin-center.spec.ts`
- `admin/vitest.config.ts`、`admin/tests/plugin-center.spec.ts`
- 两端 `package.json`、锁文件

## 架构、兼容与回滚

只增加开发期测试依赖，不改变生产 API 或路由；移除测试入口即可回退。

## 测试与构建结果

- `cd web && pnpm test:component` 通过。
- `cd admin && pnpm test:component` 通过。
- Web/Admin production build 通过。

## 已知限制与下一阶段

首批测试覆盖新插件中心的稳定状态；既有业务页继续由浏览器工作流覆盖。下一阶段统一响应式 Shell 模式。
