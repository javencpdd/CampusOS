# CampusOS v0.9.16-dev：A15 多实例约束与最终验收

> 实现状态：已完成  
> 验收状态：已验收（既有工作树空白告警已记录）

## 阶段目标

防止 Local 文件/SQLite 在多写实例中静默失效，并完成 v0.9 的数据、浏览器和恢复验证。

## 实际完成内容

- 新增 `CAMPUSOS_INSTANCE_MODE`；`multi` 在启动前明确拒绝，因为 Local User Storage 和 v1 SQLite KV 未具备多写安全性。
- 文档化单实例部署限制与未来共享 Provider 迁移方向。
- 应用 `000021`、`000022`，完成隔离 restore drill 与五视口真实回归。
- 更新 Admin `/architecture` 的 38 张表视图、迁移时间线和 v2 插件文件目录说明；架构同步检查已通过。

## 主要修改目录和文件

- `pkg/config/config.go`、`internal/server/deployment.go`
- `.env.example`、部署与插件文档
- `migrations/000021_v09_plugin_market.*`、`000022_v09_plugin_catalog_permissions.*`

## 架构、兼容与回滚

单实例配置仍为默认；禁止模式在启动前失败，不会误以为多实例安全。生产回滚优先回退应用并保留新增表/文件。

## 测试与构建结果

- Go 全量测试、Web/Admin/docs-site/SDK build、architecture/contracts/database/docs-links/frontend-budget 全部通过。
- `campusos-data-architecture-sync` 确认 22 个迁移、38 张 schema 表和 Admin 架构视图一致。
- 迁移状态为 22 个已应用迁移；隔离恢复演练恢复 users=5、threads=33、plugins=9、migrations=22。
- 五视口 Chrome 流程分组通过并生成 60 张截图。
- 全局 `git diff --check` 仅报用户既有 `docs/Todo/sys` 行尾空白；v0.9 改动未引入该问题，未修改用户文件。

## 已知限制与下一阶段

不包含远程市场、共享文件 Provider、自动灰度执行、向量搜索或第三方 Secret 迁移。后续版本应在共享存储和隔离运行时成熟后再开放多实例写入。
