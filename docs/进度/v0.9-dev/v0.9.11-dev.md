# CampusOS v0.9.11-dev：A10 本地 Catalog 与包治理

> 实现状态：已完成  
> 验收状态：已验收

## 阶段目标

为管理员已安装的 v2 External Plugin 建立本地目录、风险信息、请求与导入治理。

## 实际完成内容

- 同步 v2 External Plugin 到 Catalog，保留管理员设置的 draft/published/hidden 可见性。
- 提供用户安装请求和管理员审核记录；请求不会自动安装宿主代码。
- 复用包预检、摘要、冲突检查与既有 snapshot/rollback；Resource Package 不会进入 v2 Catalog。

## 主要修改目录和文件

- `internal/plugin/market.go`、`internal/plugin/market_pg.go`
- `internal/plugin/package.go`、`internal/plugin/handler.go`
- `migrations/000021_v09_plugin_market.*`

## 架构、兼容与回滚

Catalog 下架不卸载插件，包卸载不默认删受管数据；历史 builtin/v1 仍由兼容 Facade 管理。

## 测试与构建结果

- 包导入预检、重复/冲突处理及市场服务测试通过。
- CLI 旧示例和 v2 Manifest inspect 均通过。

## 已知限制与下一阶段

本地目录不下载远程包、不交易、不评分。下一阶段提供管理员中心。
