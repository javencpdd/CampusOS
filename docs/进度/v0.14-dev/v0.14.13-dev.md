# v0.14.13-dev：版本命名遗漏审查与文档路径统一

> 阶段：`v0.14-dev`  
> 日期：2026-08-31  
> 更新时间：2026-08-31 22:01（Asia/Shanghai）  
> 状态：已完成；维护任务，不改变发布基线或历史迁移语义。

## 1. 本次目标

修复项目计划命名统一后遗留的用户可见版本文案和文档路径：项目发布/计划版本统一写为
`v0.N`，同时防止把 API、迁移、脚本、基线 schema 等技术标识错误改写。

## 2. 完成内容

| 范围 | 处理结果 |
| --- | --- |
| Admin 架构视图 | `SystemArchitectureView.vue` 的 33 个表说明和迁移时间线标题统一为 `v0.9` 至 `v0.14`；相邻的 `0000xx_vNN_*.sql` 文件名保持不变。 |
| Admin 开发文档入口 | 将“v1-v13 的迭代主线”修正为“v0.1–v0.14”。 |
| 文档路径 | 将 `docs/help/`、`docs/api/`、`docs/architecture/` 中 42 个以 `v10`—`v14` 开头的发布文档重命名为 `v0.10`—`v0.14`，并同步仓库内链接、docs-site GitHub 链接和 Prometheus Runbook 路径；另修正一份 `v4` 进度文件路径。 |
| 生成合同 | `campusos-contracts` 的 v0.13 错误目录与可观测指标目录均迁到 `docs/api/v0.13*.md`，H1 同步为 `CampusOS v0.13 …`，并重新生成且通过漂移检查。 |
| 历史计划/进度 | 修正 `V8-A*`、`v8`、`v11/v12/v13` 等人类可读发布版本残留；保留历史路线图中“未来 v1.0”这一当时设想，不伪造为 `v0.1.0`。 |
| CLI 与脚本输出 | 将可见帮助、注释与验收输出中的历史阶段标签改为 `v0.10`—`v0.14`；脚本文件名、Make target、夹具和迁移文件名不改。 |
| 命名边界 | 在 `docs/README.md` 明确人类可读版本与技术标识的区分规则。 |

## 3. 点名文件的处理结论

- `admin/src/modules/architecture/pages/SystemArchitectureView.vue`：已修正所有可见的发布阶段标题；迁移文件字段仍保留 `v13`、`v14`，因为它们是已存在的文件名。
- `docs/help/系统设计相关/v14学期治理与个人文档使用说明.md`：已迁移为
  `docs/help/系统设计相关/v0.14学期治理与个人文档使用说明.md`，并更新全部入链。
- `docs/进度/v0.14-dev/v0.14.1-dev.md`：H1 与产品阶段文字原本已正确为 `v0.14`；其中
  `campusos.v14-g0-baseline/v1`、`baselines/v14-g0.json`、`test-v14-*`、`.cache/go-v14` 是
  schema、历史证据、命令或缓存标识，按规则保留。

## 4. 修改范围

- `admin/src/modules/architecture/pages/SystemArchitectureView.vue`
- `admin/src/modules/docs/pages/DeveloperDocsView.vue`
- `cmd/campusos-contracts/main.go`、`cmd/campusosctl/resource_commands.go`
- `scripts/` 中的版本化验证输出和历史夹具说明
- `docs/README.md`、`docs/help/`、`docs/api/`、`docs/architecture/`、项目计划、进度记录与 `docs-site/` 的引用
- `deploy/prometheus/campusos-v13.rules.yml` 的 Runbook 路径

## 5. 验证结果

| 命令 | 结果 |
| --- | --- |
| `go run ./cmd/campusos-contracts --write`，随后 `--check` | 通过；所有受管 API 合同无漂移。 |
| `go test ./cmd/campusos-contracts ./cmd/campusosctl -count=1` | 通过。 |
| `docker compose … exec -T admin pnpm build` | 通过；仅有第三方依赖的 Rollup `#__PURE__` 注释提示。 |
| `docker compose … exec -T docs pnpm build` | 通过。 |
| `python scripts/check-doc-links.py` | 通过，511 个本地链接有效。 |
| `bash -n scripts/*.sh` | 通过。 |
| `git diff --check`、`python scripts/check-line-endings.py --include-untracked` | 本记录创建后复跑。 |

## 6. 后续任务

- 继续保留 `v1.1` 作为尚未立项的下一主线名称；不要把它改写为历史 `v0.N`。
- 新增用户可读的计划、Help、API 或架构文档时使用 `v0.N`；新增技术合同/脚本/迁移标识时先判断是否属于稳定机器标识。

本次改动保持未提交、未推送。
