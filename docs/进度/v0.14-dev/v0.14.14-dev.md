# CampusOS v0.14.14-dev：ER 图生成器 SQL 注释兼容修复

> 日期：2026-09-03
> 状态：已完成

## 1. 本次目标

修复 ER 图生成器解析迁移文件时，将 SQL 行注释误判为列定义的问题，并确保完整生成流程可用。

## 2. 完成内容

- 在 SQL 解析入口安全移除行注释和块注释，同时保留单引号、双引号及 dollar-quoted 内容。
- 修复 Graphviz 对空字段标记 `<FONT>` 标签的兼容性问题。

## 3. 修改文件

- `migrations/tool/generate_er.py`
- `docs/进度/v0.14-dev/v0.14.14-dev.md`

## 4. 验证结果

- `python3 migrations/tool/generate_er.py --migrations migrations --out migrations/er` 通过。
- 解析 77 张表、43 个外键，并成功渲染 10 个 SVG 文件。
- 生成器输出包含 `schema.json`、`relations.md` 和 DOT/SVG 文件。

## 5. 后续任务

- 使用项目实际 Python 环境中的 `python` 命令运行同一生成命令即可；当前系统终端仅提供 `python3`。
