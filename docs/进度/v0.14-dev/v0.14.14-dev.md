<<<<<<< HEAD
# CampusOS v0.14.14-dev：ER 图生成器 SQL 注释兼容修复

> 日期：2026-09-03
> 状态：已完成

## 1. 本次目标

修复 ER 图生成器解析迁移文件时，将 SQL 行注释误判为列定义的问题，并确保完整生成流程可用。

## 2. 完成内容

- 在 SQL 解析入口安全移除行注释和块注释，同时保留单引号、双引号及 dollar-quoted 内容。
- 修复 Graphviz 对空字段标记 `<FONT>` 标签的兼容性问题。

## 3. 修改文件

- `migrations/tool/generate_er.py`
- `docs/进度/v0.14-dev/v0.14.14-dev.md`

## 4. 验证结果

- `python3 migrations/tool/generate_er.py --migrations migrations --out migrations/er` 通过。
- 解析 77 张表、43 个外键，并成功渲染 10 个 SVG 文件。
- 生成器输出包含 `schema.json`、`relations.md` 和 DOT/SVG 文件。

## 5. 后续任务

- 使用项目实际 Python 环境中的 `python` 命令运行同一生成命令即可；当前系统终端仅提供 `python3`。
=======
# v0.14.14-dev：历史项目计划集中迁移与引用验证

> 阶段：`v0.14-dev`  
> 日期：2026-09-01  
> 更新时间：2026-09-01（Asia/Shanghai）  
> 状态：已完成；文档与开发工作流维护，不改变 `v0.13.0` 发布基线、v0.14 Final 门禁或历史计划内容。

## 1. 目标

将 `v0.1` 至 `v0.14` 的历史项目计划从原有分散目录集中迁移到
`docs/项目计划书v0/项目计划v0.N/`，同时保留版本子目录、逐项修复所有仓库内入站和出站路径，并验证迁移后链接和开发工具仍可用。

## 2. 完成内容

| 范围 | 结果 |
| --- | --- |
| 计划文件 | 迁移 14 个版本目录、74 个文件；未扁平化，保留 `项目计划v0.1` 至 `项目计划v0.14` 的原有层次。 |
| 依赖梳理 | 迁移前识别 152 个引用计划目录的仓库文件：`docs/` 140、`docs-site/` 2、根 README 1、`scripts/` 3、`skills/` 6。 |
| 相对链接 | 重算移动计划指向 `进度/`、`help/`、`api/`、`architecture/` 和仓库根的相对路径；6 个跨版本同级计划链接继续使用 `../项目计划v0.N/`。 |
| 文档入口 | 新增 [历史计划归档 README](../../项目计划书v0/README.md)，并从 `docs/README.md` 提供发现入口。 |
| 脚本与 Skills | 文档链接检查器覆盖整个新根目录；v0.13 容量脚本、工作流 Skill、动态任务检查脚本、导览 Skill 和 Docs 站路径均已更新。 |
| 原有问题 | 修复 v0.1 项目介绍中原有的根 `LICENSE` 相对路径，使迁移后可正确解析。 |

## 3. 验证结果

| 命令 | 结果 |
| --- | --- |
| `python scripts/check-doc-links.py` | 通过；覆盖全量历史计划目录及相关 Docs/Docs 站/Skills 本地链接。 |
| `python skills/sources/campusos-readme-update/scripts/audit_readme_structure.py --root .` | 通过；仅报告 README 行数与直接文档链接数量的维护提醒。 |
| `python skills/sources/campusos-readme-update/scripts/check_readme_links.py --root . README.md docs/README.md` | 通过。 |
| `docker compose --env-file deploy/docker/.env.dev.local -f compose.dev.yml exec -T docs pnpm build` | 通过；VitePress 静态站构建完成。 |
| `git diff --check` | 通过。 |
| `python scripts/check-line-endings.py --include-untracked` | 通过；未发现换行策略违规。 |

## 4. 迁移边界

- 不保留旧目录的复制副本或重定向文件，避免形成重复权威；所有受影响的仓库内引用均指向新根目录。
- 不改写历史计划的业务结论、技术标识、迁移文件名或协议版本；仅调整路径及修复已检测到的失效相对链接。
- 本次未提交、未推送。
>>>>>>> 2c863bbf94c376e5417b993a84aba12e84f85f0d
