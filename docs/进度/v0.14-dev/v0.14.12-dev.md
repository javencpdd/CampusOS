# v0.14.12-dev：迁移链冗余审查与使用说明

> 阶段：`v0.14-dev`  
> 更新时间：2026-08-31  
> 状态：已完成（文档与审查任务；未修改任何历史迁移 SQL）

## 本次目标

审查 `migrations/` 的文件配对、演进边界、潜在索引/约束冗余和回退风险，并在迁移目录提供可供
Windows、Linux、WSL 与 Git Bash 开发者直接使用的说明。

## 审查结果

- `000001` 至 `000049` 共 49 组 `up/down` 文件，配对完整，无缺号或孤立 SQL 文件。
- 架构同步检查确认迁移元数据、`000049` 最新版本和管理端数据架构视图的 78 张表一致；未发现
  缺失或陈旧的表映射。
- 开发数据库的结构合同、数据审计与索引卫生检查均返回 0 项违规；没有活动的重复约束或可直接
  删除的冗余 B-tree 索引。
- `000021/000022` 的兼容字段演进、`000037` 的投递状态扩展、`000041` 的历史索引清理和
  `000048` 的约束名称兼容桥均属有意保留的历史步骤，不应合并、删除或重写。
- v0.14 存储、个人文档、学期和课程表对象绑定迁移保持一个迁移一个业务边界，便于审计和隔离
  演练；不存在可安全压缩为单个文件的重复实现。

## 交付

- 新增 [`migrations/README.md`](../../../migrations/README.md)，包含当前审查结论、迁移运行方式、
  `down/reset` 的生产安全边界、Windows 与 Linux 命令、可执行检查及未来执行器增强建议。
- 明确约定：真实数据环境只允许追加向前修复迁移；`down` 和 `reset` 只可用于可销毁的隔离演练库。

## 验证证据

```text
python skills/sources/campusos-data-architecture-sync/scripts/check_architecture_sync.py --root .
# migrations: 49；schema tables: 78；architecture view tables: 78；missing/stale: none

POSTGRES_CONTAINER=campusos-dev-postgres-1 ./scripts/database-check.sh all
# 数据审计：0 项违规；结构合同：通过；索引卫生：0 项问题

# 依次运行 v0.14 G0、学期、存储对象、课程表学期引用、个人文档、
# 课程表对象绑定和历史夹具的隔离库 up/down/up 演练
# 7 个脚本均以退出码 0 通过
```

## 后续边界

本次不重写任何已可能执行过的迁移，也不执行真实数据的 `--apply`、`down` 或 `reset`。如需进一步
优化，应单独立项实现按版本安全回退、文件校验和、advisory lock 与事务/非事务迁移策略，且保持
现有迁移链不变。
