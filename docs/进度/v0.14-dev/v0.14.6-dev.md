# v0.14.6-dev：v0.14 当前状态文档收口

> 阶段：`v0.14-dev`
> 日期：2026-08-30
> 更新时间：2026-08-30 01:21（Asia/Shanghai）
> 发布基线：`v0.13.0`；本文是文档与上手入口收口记录，不代表 `v0.14.0 Final` 已发布。
> Git：本次仅修改工作树，未执行 commit 或 push。

## 1. 目标

在 P0/P1 代码实现已具备证据的前提下，统一仓库计划总结、Help、官方文档站、文档入口和项目上手 Skill 的
当前状态表述。避免继续把 v0.14 写成“尚未开始”或把已经实现的能力误导为已经完成 Final 发布。

## 2. 实际完成

| 范围 | 完成内容 | 结果边界 |
| --- | --- | --- |
| 计划总结与 Help | 将 v0.14 改为“P0/P1 已实现、发布基线仍为 v0.13.0、Final 门禁未通过”，并保留 v0.1-v0.13 历史材料的追溯定位。 | 历史计划文件名与历史结论不重写；它们不是当前待办。 |
| 仓库文档入口 | `docs/README.md`、`docs/进度/README.md` 指向本记录，并把 `v0.14.5-dev` 保留为 P1 代码与验证证据。 | 最新记录不取代已有实现证据或发布审查。 |
| 官方文档站 | 版本演进、文档生命周期、入门页与侧栏统一显示 `v0.1-v0.14`；仓库内进度/计划以代码路径说明，避免把非 docs-site 的 Markdown 当作站内路由。 | 文档站不将仓库内计划文件当作可部署页面。 |
| 项目 Skills | 项目上手说明和无提交开发流程参考更新到最新记录，明确 `000001-000049` migration、已实现 P0/P1 与 Final 门禁。 | 未改变任何 Skill 发现桥、`SKILL.md` 指令或运行行为。 |

## 3. 当前 v0.14 结论

- 已有 P0/P1 证据：AcademicTerm、Storage Object Layer、课表受管写入、历史 dry-run/adoption、默认只读对账、
  Personal Documents、安全内容编辑核心、owner-scoped Document ReadPort 与低基数运营指标。具体代码和测试见
  [v0.14.5-dev](v0.14.5-dev.md)。
- 当前发布基线仍是 `v0.13.0`，不能把开发工作树描述为 v0.14 Final。
- Final 仍须获得：授权管理员对真实历史数据 `--apply` 的决定和证据、Linux 目标环境的 release-check/浏览器验证、
  Converter Runner 的安全评审与验收、正式发布审查。

## 4. 主要修改文件

- `docs/计划书总结/`、`docs/help/计划书总结/`：当前规划、计划总结和 Help 状态同步。
- `docs/README.md`、`docs/进度/README.md`：仓库文档与进度入口同步。
- `docs-site/project/`、`docs-site/index.md`、`docs-site/guide/getting-started.md`、`docs-site/.vitepress/config.mts`：官方文档站入口和导航同步。
- `skills/guides/CampusOS-项目上手Skill使用说明.md`、
  `skills/sources/campusos-project-onboarding/references/current-project-status.md`、
  `skills/sources/campusos-dev-nocommit/references/campusos-versioned-dev.md`：项目内 Skill 上手信息同步。

## 5. 验证与环境限制

已执行文档链接、README、Skill bridge、脚本语法、行尾和差异检查；具体命令与结果见本节末尾的
“验收结果”。本次没有修改 Go、前端、migration 或 Docker 配置，因此不重复宣称 P1 的源码测试；其原始证据保留在
`v0.14.5-dev`。

Docker Desktop daemon 当前未运行（Windows named pipe 不存在），且本机 `docs-site/node_modules` 为空；因此没有在
本轮启动容器，也没有执行 docs-site 生产构建。恢复 Docker 后应补执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\docker-dev.ps1 test
docker compose --env-file deploy/docker/.env.dev.local -f compose.dev.yml exec -T docs pnpm build
```

### 验收结果

| 检查 | 结果 |
| --- | --- |
| `python skills/sources/campusos-readme-update/scripts/audit_readme_structure.py --root . --require-doc docs/进度/v0.14-dev/v0.14.6-dev.md` | 通过；README 结构与指定进度入口可达。 |
| `python skills/sources/campusos-readme-update/scripts/check_readme_links.py --root . README.md docs/README.md` | 通过。 |
| `python scripts/check-doc-links.py` | 通过，检查 506 个本地链接。 |
| `python skills/sources/campusos-skill-repository-sync/scripts/sync_skill_bridges.py --root . --check` | 通过；8 个规范源，无 bridge 漂移。 |
| `& 'C:\Program Files\Git\bin\bash.exe' -n skills/sources/campusos-project-onboarding/scripts/context_snapshot.sh` | 通过。Windows 自带 WSL Bash 因服务访问被拒绝，改用 Git Bash 完成静态语法检查。 |
| `python scripts/check-line-endings.py --include-untracked` | 通过；`fixed=0`、`errors=0`。 |
| `git diff --check` | 通过。 |

Windows 当前未安装 `make`，故未直接执行 `make readme-check` / `make docs-links`；上表使用它们对应的仓库脚本，
验证范围相同。

## 6. 回滚与后续

- 本条仅修改 Markdown、VitePress 导航文本和项目内 Skill 的参考材料；如需回退，整体回退这些文档改动即可，
  不涉及数据、迁移、运行服务或用户文件。
- 后续 Final 工作必须在具备 Docker/Linux 目标环境和授权操作人的条件下完成，不应以文档同步或本机静态检查替代。
