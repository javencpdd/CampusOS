# v0.14.1-dev：G0 真实基线冻结

> 日期：2026-08-28
> 更新时间：2026-08-28 22:20（Asia/Shanghai）
> 状态：已完成
> 范围：基线采集、迁移隔离演练、当前文档与仓库 Skill 状态校正

## 1. 本次目标

完成 v14 计划的 G0：以可复现的机器快照和隔离迁移演练确认当前 v0.13.0 发布基线，消除当前文档、项目
上手 Skill 与实际 migration 数量之间的漂移；不提前实现或宣称 v14 业务功能。

## 2. 完成内容

| 项目 | 实际结果 |
| --- | --- |
| 基线工具 | `campusos-baseline` 升级为 `campusos.v14-g0-baseline/v1`，记录工具版本、Personal Space 文件/容量/符号链接/未知路径和课表 JSON 健康度；采集过程只读且不暴露用户文件路径。 |
| 基线证据 | 新增 `baselines/v14-g0.json`：43 个 migration、22 个 Module、282 条已生成路由、4 个个人空间目录、15 个文件、12,232,636 字节、0 符号链接、1 个有效课表 JSON；工作树当时为 dirty，事实已明确记录。 |
| migration 演练 | 新增隔离脚本，空库顺序应用 `000001-000043`，验证 `000043` 的 `posts.parent_floor_number` down/up，再运行 Schema、审计和索引卫生检查；自动删除名称为 `campusos_v14_g0_*` 的测试数据库。 |
| 当前文档 | 当前数据库入口与官方文档从“42 个 migration / 000042”校正为“43 个 migration / 000043”；v14 状态明确为“G0 完成，功能未交付”。 |
| 项目 Skill | 更新 onboarding 与开发流程 Skill 的 v14 快照、最新 migration、当前进度入口及使用说明；重新生成 `.agents/skills` 桥接，未安装用户级 Skill。 |

## 3. 修改文件

| 范围 | 文件 |
| --- | --- |
| 基线 | `cmd/campusos-baseline/main.go`、`baselines/v14-g0.json` |
| migration 验证 | `scripts/test-v14-g0-baseline-migration.sh` |
| 当前文档 | `docs/项目计划v14/00-v14版本计划书.md`、`docs/README.md`、`docs/进度/README.md`、`docs/help/系统设计相关/数据库管理指南.md`、`docs/计划书总结/01-当前项目规划与后续路线.md`、`docs/help/计划书总结/02-当前项目状态与未来规划.md`、`docs-site/project/current-roadmap.md`、`docs-site/guide/architecture.md`、`docs-site/operations/database-migration-hygiene.md` |
| Skill | `skills/sources/campusos-project-onboarding/`、`skills/sources/campusos-dev-nocommit/`、`skills/sources/campusos-skill-repository-sync/scripts/sync_skill_bridges.py`、`skills/guides/`、`.agents/skills/` |

## 4. 验证结果

| 命令 | 结果 |
| --- | --- |
| `$env:GOCACHE = (Join-Path $PWD '.cache\\go-v14'); go test ./cmd/campusos-baseline -count=1` | 通过。 |
| `go run ./cmd/campusos-baseline --root .` | 通过；生成本记录所附的 G0 JSON 快照。 |
| `go run ./cmd/campusos-contracts --check` | 通过；Route、OpenAPI、权限和错误合同无漂移。 |
| `python scripts/check-architecture-boundaries.py`、`check-frontend-boundaries.py`、`test-architecture-checks.py`、`check_architecture_sync.py --root .` | 通过；43 个 migration、69 张 Schema 表与 Admin 架构视图一致。 |
| `python scripts/check-reliability-boundaries.py` | 通过。 |
| `bash -n scripts/test-v14-g0-baseline-migration.sh`，随后 `bash scripts/test-v14-g0-baseline-migration.sh` | 通过；使用 Docker PostgreSQL 容器 `campusos-dev-postgres-1`，隔离库自动清理。 |
| `python .../sync_skill_bridges.py --root . --write` 与 `--check` | 通过；8 个 canonical Skill、无桥接漂移。 |
| 官方 `quick_validate.py` 对 8 个 `skills/sources/*` 与 8 个 `.agents/skills/*` | 全部通过；本机 Python 缺 PyYAML，校验依赖仅临时安装在系统临时目录，未修改用户级 Skill。 |
| `python scripts/check-doc-links.py`、`git diff --check`、`python scripts/check-line-endings.py --include-untracked` | 通过；499 个文档链接，1498 个文本文件、30 个二进制文件，0 错误。 |

## 5. 后续任务

1. 实现 `core.academic-term`、migration `000044`、管理员 Term API/权限/审计与 Admin 管理台。
2. 再实现 User Storage Object Layer、持久 quota ledger 与 Reservation；新 migration 必须再次使用数据架构同步 Skill。
3. 在 Object Port 通过后改造 Schedule，再新增 Personal Documents、版本与受限预览。

本次改动保持未提交、未推送。
