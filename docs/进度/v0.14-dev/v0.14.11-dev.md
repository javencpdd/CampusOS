# v0.14.11-dev：开发阶段收尾与项目计划命名统一

> 阶段：`v0.14-dev`
> 日期：2026-08-31
> 更新时间：2026-08-31 13:34（Asia/Shanghai）
> 状态：已完成；项目所有者已确认当前代码验证完成，`v0.14-dev` 开发阶段收尾。
> 发布基线：仍为 `v0.13.0`。本记录不伪造真实数据操作或目标环境的发布审计；`v0.14.0 Final` 的发布证据应由 Release Owner 单独归档。
> Git：使用 `campusos-dev-nocommit`；本次不执行 commit 或 push。

## 1. 本次目标

完成 v0.14-dev 文档收尾，并统一历史项目计划的版本命名：原 `项目计划v1` 至 `项目计划v14` 改为
`项目计划v0.1` 至 `项目计划v0.14`。该规则为后续独立的 `v1.1` 计划保留清晰命名空间。

## 2. 完成内容

| 范围 | 完成内容 |
| --- | --- |
| 项目计划目录 | 重命名 14 个 `docs/项目计划vN/` 目录为 `docs/项目计划v0.N/`。 |
| 计划文件与附件 | 随 14 个目录迁移 74 份计划文件/证据附件，并重命名两份跨版本计划总结文件；链接、代码块路径和检查脚本均同步到新路径。 |
| 版本语义 | 计划书、计划总结、文档门户、官方文档站与仓库 Skill 将 CampusOS 历史版本统一写为 `v0.1` 至 `v0.14`。技术协议版本（如 `/api/v1`、Host API v1/v2、Manifest v2、CampusDoc v1）保持不变。 |
| v0.14 收尾 | 以本记录作为代码开发收尾证据；后续 `v1.1` 需独立立项、冻结计划和进度目录，不能与历史 `v0.1` 混淆。 |

## 3. 修改范围

- `docs/项目计划v0.1/` 至 `docs/项目计划v0.14/`
- `docs/计划书总结/`、`docs/help/计划书总结/`、`docs/README.md`、根 `README.md`
- `docs-site/project/`、相关 Help/API/architecture/历史进度链接
- `scripts/check-doc-links.py`、v0.13 容量检查脚本和仓库内 CampusOS Skill 引用

## 4. 验证结果

| 检查 | 结果 |
| --- | --- |
| `python scripts/check-doc-links.py` | 通过，511 个本地文档链接可解析。 |
| README 结构与链接检查 | 通过；根 README 的长度/直链数量仅为维护建议，不是失败。 |
| `python scripts/check-line-endings.py --include-untracked` | 通过；`text=1579`、`binary=30`、`errors=0`。 |
| `git diff --check` 与 `git diff --cached --check` | 通过；无空白错误。 |
| `bash -n scripts/check-v13-capacity-gate.sh scripts/v13-capacity-drill.sh` | 通过；已同步的计划证据路径可被 Shell 解析。 |
| Skill 同步与校验 | `sync_skill_bridges.py --write/--check` 通过，8 个规范源无桥接漂移；16 个规范源/桥接目录均通过 Skill 快速校验。 |
| `docker compose --env-file deploy/docker/.env.dev.local -f compose.dev.yml exec -T docs pnpm build` | 通过；VitePress 1.6.4 构建完成。 |

文档格式处理曾异常清空本轮涉及的工作副本文本，已立刻按 Git 索引精确恢复 344 个受影响的已跟踪文件；随后重新应用本次命名迁移并完成上述全量复验。图片文件未被修改。

## 5. 后续任务

- 若要正式发布 `v0.14.0 Final`，由 Release Owner 归档真实历史数据 apply、Linux 目标环境、恢复/浏览器、最终基线和规模性能证据。
- `v1.1` 是下一阶段预留版本，尚未创建计划目录或实施任务。
