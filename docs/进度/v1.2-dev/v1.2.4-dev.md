# v1.2.4-dev 进度：Help 文档全量核验与失效指南清理

> 日期：2026-09-26
> 范围：文档审计与修订；不包含 v1.2 产品功能实现，也不改变数据库/API。

## 本阶段目标

逐一核对 `docs/help/` 文档与当前代码、配置、路由、迁移和命令，清理会误导新开发者的旧指南。

## 实际完成

- 对原有 77 份 Help Markdown 建立逐文件状态表，见
  [全量核验与删除理由](../../help/2026-09-26全量文档核验.md)。
- 删除 11 份过时文档：v0.5 Smoke、v0.5 集成、v0.6 数据库体检、单机 PostgreSQL 事故、
  旧 RBAC、v0.10 权限、旧插件包治理、v0.6 插件工具链、旧个人主页风格包、未实现的单文件分发方案、
  v1.0 旧插件数据库重置说明。
- 更新仍有价值的身份、可靠任务、板块、课表与文档、插件、AI、Resource Package 指南；
  将旧 `0000xx` 迁移编号标为历史，移除当前库不应执行的旧迁移演练和不存在的 `v14-migration-check`。
- 校正 `docs/help/README.md`、开发者路线、`docs/README.md` 与 `docs/api/API索引.md` 的引用；
  旧进度/计划中曾直接链接被删指南的地方改指当前入口，保持链接可用。

## 主要目录与架构变化

`docs/help/` 为主要变更；少量链接修正在 `docs/api/`、`docs/进度/v0.6-dev/`、
`docs/项目计划书v1/`。没有修改 Go、Vue、数据库或运行时架构。

## 兼容、迁移与回滚

代码和数据无迁移。旧文档仍可在 Git 历史查阅；需要撤回本次文档修订时可按文件审查后单独恢复，
不得把已失效的迁移指令重新放回当前入门路径。用户工作区的其他改动未清理。

## 验证

- `make docs-links`：通过，995 个本地链接（含新增审计和本进度文档）。
- `cd docs-site && pnpm build`：通过，VitePress 站点构建完成。
- `make readme-check`：通过。
- `git diff --check`：通过。
- 自动核查 Help 中引用的 Make target：无不存在的 target。
- `python3 scripts/check-line-endings.py --include-untracked`：未通过；五个既有 PowerShell 文件含 bare LF，
  均不在本阶段变更范围，本次没有擅自改写它们。

## 已知限制与下一步

本阶段是仓库静态核验，不等于生产环境、Windows 实机或真实邮件/模型服务回归。
下一阶段应由对应模块负责人在功能或配置变动时维护唯一权威指南，并持续区分
v1.2 计划和已交付能力。
