# v1.2.5-dev 进度：高级开发 Skill 的 v1.2 调用说明

> 日期：2026-09-26
> 状态：已完成
> 范围：Skill 使用文档；不代表任何 v1.2 实施阶段已经完成

## 1. 本次目标

说明如何组合 `$senior-developer` 与 `$campusos-dev-nocommit`，按照 v1.2 正式计划开始或继续开发，避免
“完成全部任务”式调用跳过阶段依赖、验收证据和安全边界。

## 2. 完成内容

- 明确两个 Skill 的职责：前者约束工程方法，后者负责版本阶段、进度证据、验证和未提交交付。
- 新增“第一次开始”“中断后继续”“指定 V12-G1”三种可直接复制的调用示例。
- 明确当前计划仍为待实施，首次实现应从 `V12-G0` 开始；继续开发时选择依赖已满足的首个未完成阶段。
- 明确每次只完成一个阶段或一个可独立验收切片，并分别记录实现、自动化验收和目标环境验收。

## 3. 修改文件

- `skills/guides/高级开发工程师专家包使用与接入说明.md`
- `skills/guides/README.md`
- `docs/进度/v1.2-dev/v1.2.5-dev.md`

没有修改 Skill 规范源或 `.agents/skills` 桥接，因此不需要生成新的桥接内容。

## 4. 验证结果

收尾时记录 Skill 校验、桥接检查、文档链接、换行和 `git diff --check` 的实际结果。

## 5. 后续任务

后续真正开始 v1.2 实现时，按本指南先执行 `V12-G0`；本次仅完善调用说明，不修改计划实施状态。
所有改动保持未提交、未推送。
