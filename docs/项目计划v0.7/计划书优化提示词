请先审查并修订现有计划书：

`/home/jack/bbs/bbs01/CampusOS/docs/项目计划v0.7/00-v7版本计划书.md`

当前计划整体方向正确，但范围偏大、部分阶段顺序和验收标准需要优化。请先完善计划书，再继续实施，不要直接进行一次性大规模重构。

## 一、v0.7 目标范围

v0.7 聚焦以下内容：

* 模块化单体边界优化
* Module Kernel 与 Bootstrap 拆分
* Core Module、Built-in Feature、External Plugin、Resource Package 分类
* Moderation Core
* User Storage Core
* Built-in Feature Registry
* Appearance Facade
* Plugin Platform 渐进拆分
* Resource Package 基础合同
* Agent、Runner、Knowledge、MCP、Secret 的安全接口边界

本轮 Agent 只完成接口、权限、安全策略和扩展合同，不实现完整 Agent Center、知识库管理、人格库、提示词助手、任务执行或代码执行器。

## 二、调整实施阶段

请将计划调整为以下顺序：

```text
A0    计划、模块分类和数据所有权
A0.5  当前系统基线冻结
A1    Module Kernel
A2a   基础设施 Bootstrap 拆分
A2b   业务模块装配拆分
A3    Ports、依赖规则和模块边界检查
A4    Moderation Core
A5a   User Storage Port 与旧实现 Adapter
A5b   RichText、Schedule 切换到 User Storage
A5c   User Storage 实现归属迁移
A6    Built-in Feature Registry
A7    Resource Package 合同与 Legacy Resource Source
A8    Appearance Facade
B1a   Plugin Catalog 与 Runtime Registry
B1b   Plugin Lifecycle 与 Config
B1c   Plugin UI、Event、Audit Registry
B2    Agent、Runner、Knowledge、MCP、Secret 合同
Final 全链路验收
```

## 三、Module Kernel

Module 使用基础接口和可选生命周期接口，避免所有模块实现无意义的空方法：

```go
type Module interface {
    ID() string
    Dependencies() []string
    Register(*AppContext) error
}

type Starter interface {
    Start(context.Context) error
}

type Stopper interface {
    Stop(context.Context) error
}

type HealthReporter interface {
    Health(context.Context) Health
}
```

要求：

* 模块 ID 稳定且唯一
* 启动前检查依赖缺失和循环依赖
* 按拓扑顺序注册和启动
* 启动失败时逆序停止已启动模块
* AppContext 只暴露明确注册的 Port，不得成为全局 Service Locator
* External Plugin 不得获取 AppContext 或注册 Core Module

## 四、Built-in Feature 启停

Feature 必须声明：

```text
always-on
restart
hot-gated
```

规则：

* Core Module 使用 `always-on`
* Built-in Feature 首版默认使用 `restart` 或 `hot-gated`
* 禁止运行时动态增删 Gin 路由
* `hot-gated` 使用统一中间件拒绝已停用功能的 API
* 停用功能只隐藏 UI、拒绝 API、停止事件处理，不得删除数据

## 五、重点模块改造

### Moderation

将以下能力作为始终启用的 Core：

* 权限策略
* 板块作用域
* 审计
* 数据完整性校验

以下内容作为可配置功能：

* `allow_pin`
* `allow_lock`
* `allow_delete_post`
* 相关管理 UI

### User Storage

采用渐进迁移：

1. 定义 User Storage、Quota、SafePath、AssetStore 等 Port
2. 使用现有 Personal Space 文件实现作为兼容 Adapter
3. 将 RichText 和 Schedule 改为依赖 User Storage Port
4. 验证稳定后再移动具体实现目录
5. 保持原有文件路径、URL 和用户数据不变

### Built-in Feature

将以下功能纳入统一 Feature Registry：

* Personal Space
* Controlled RichText Article
* Personal Schedule
* Appearance

这些功能可启停但不可作为普通插件安装或卸载。

### Appearance

v0.7 只建立统一 Facade，不要一次性物理合并全部代码：

```text
AppearanceCatalog
PackageValidator
PreferenceStore
AppearancePolicy
```

现有 homepage、webtheme、stylepack 和 personal-space style 暂时通过 Adapter 接入。

### Resource Package

新增统一 Resource Repository，并保留旧目录兼容读取。

目标目录：

```text
data/resources/
├── themes/
├── homepage-packs/
├── space-style-packs/
├── skills/
├── prompts/
├── personas/
└── knowledge-metadata/
```

现有 `data/plugin_data/*/style-packs` 作为 Legacy Resource Source，不立即迁移或删除。

### Plugin Platform

不要一次性重写 Plugin Manager。保留现有 Manager Facade，并逐步将职责委托给：

* Plugin Catalog
* Runtime Registry
* Lifecycle Service
* Config Service
* UI Registry
* Event Registry
* Audit/Log Service

Plugin Manager 最终只管理真正的 External Plugin。

## 六、架构边界检查

新增自动化架构检查器和 Makefile/CI 命令，至少阻止新增以下依赖：

* 业务模块直接导入其他模块的 PostgreSQL Repository
* RichText 或 Schedule 依赖 Personal Space 内部实现
* External Plugin 导入 CampusOS `internal/*`
* Agent Runner 直接访问数据库、JWT 私钥、用户 Token 或宿主完整文件系统

初期可以使用 allowlist，只阻止新增违规依赖，不要求一次性清除全部历史问题。

## 七、基线冻结

在继续重构前，增加 A0.5 基线文档，记录：

* 当前全量 Go 测试结果
* Web、Admin、docs-site 构建结果
* 路由和 OpenAPI 快照
* 数据库 migration 状态
* Plugin Manifest 和 Runtime Manifest 清单
* 当前数据目录结构
* smoke、database-check、architecture-check、release-check 结果

后续阶段必须基于这些快照验证兼容性。

## 八、阶段验收

每个阶段必须分别记录：

* 实现状态
* 验收状态
* 主要修改文件
* 测试证据
* 兼容结果
* 回滚结果
* 进度文档路径

统一 Definition of Done：

* 定向测试通过
* `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` 通过
* `git diff --check` 通过
* 架构边界检查通过
* 涉及的 Web、Admin、docs-site 构建通过
* API、路由、配置和数据变化已记录
* 兼容路径和回滚路径已验证
* 对应进度文档已生成
* v0.7 计划书状态和证据已同步

没有完整证据时，不得标记为已验收。

## 九、文档要求

每完成一个独立阶段，在以下目录创建进度说明：

`/home/jack/bbs/bbs01/CampusOS/docs/进度/v0.7-dev`

文件编号按照该目录当前最新编号继续顺延，不得覆盖已有文档。

每份进度文档至少包含：

* 阶段目标
* 实际完成内容
* 主要修改目录和文件
* 架构变化
* 兼容和迁移处理
* 测试与构建结果
* 回滚方式
* 已知限制
* 下一阶段任务

同时持续更新：

`/home/jack/bbs/bbs01/CampusOS/docs/项目计划v0.7/00-v7版本计划书.md`

确保计划、代码、测试和进度文档保持一致。

## 十、版本语义

计划中明确：

```text
P0 完成：v0.7.0-alpha
P1 完成：v0.7.0-beta
最终验收：v0.7.0
```

当前 A1、A2 已标记的状态必须重新根据实际代码、测试和进度文档核验，将“实现状态”和“验收状态”分开记录。

## 十一、禁止事项

* 禁止一次性重写整个系统
* 禁止删除旧 API、表、数据目录或兼容 Manifest
* 禁止将模块化单体拆成大量微服务
* 禁止引入反射式自动装配或全局 Service Locator
* 禁止停用 Feature 时删除用户数据
* 禁止为了通过测试移除权限、审计、路径、配额或内容安全校验
* 禁止默认明文导出任何 Token
* CampusOS Session、Access Token、Refresh Token、JWT 私钥永不导出
* 禁止 Agent Runner 或 External Plugin 直接访问核心数据库和内部 Service

先完成计划书修订和当前阶段状态核验，然后严格按照修订后的顺序继续实施。每完成一个阶段立即测试、验收并生成进度文档。

Spend time on thinking; you do not need to use the commentary channel to report progress to me.
