# v9 开始前的系统基线冻结

> 冻结日期：2026-07-13  
> 基线版本：v0.8.0 架构完成态  
> 后续契约快照：`docs/api/http-routes-v0.6.json`、`docs/api/openapi-v0.6-current.yaml`

## 已验证基线

- Go：`GOCACHE=/tmp/campusos-go-cache go test ./... -count=1` 通过。
- 前端：Web、Admin、docs-site 和 TypeScript SDK 构建通过；`make frontend-budget` 通过。
- 检查：`make architecture-check`、`make contracts-check`、`make database-check` 和 `make docs-links` 通过。
- 路由：148 条 HTTP 路由；v9 仅以追加方式扩展路由，并由 contracts 工具重新生成快照。
- 数据库：已应用 `000001` 至 `000020`；v9 不修改历史迁移。
- 插件：9 个已安装插件；`data/plugins/` 仅存运行实现，Resource Package 已在 `data/resources/` 与 Legacy `data/plugin_data/` 分离。
- 数据目录：`data/personal-space/`、`data/plugin_data/`、`data/resources/`、`data/plugins/` 均保留原有位置和兼容读取行为。

## 比较规则

1. 路由、OpenAPI 和权限目录通过 `campusos-contracts --check` 比较。
2. 新数据库对象只能使用追加迁移；回退应用时新增数据保留。
3. v1 Manifest、Host API v1 SQLite KV、Built-in Feature 和 Resource Package 均保持可用。
4. 前端入口体积采用每个 `dist/assets/index-*.js/css` 的预算门禁比较。
5. 既有工作树改动不作为 v9 回归；截至最终验收，`docs/Todo/sys` 的行尾空白会使全局 `git diff --check` 告警，v9 文件的 scoped 检查应保持通过。
