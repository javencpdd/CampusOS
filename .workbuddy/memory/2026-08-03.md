# 2026-08-03

## CampusOS 项目 Onboarding

使用 `skills/sources/campusos-project-onboarding` skill 完成项目快速了解：

- 当前基线：v0.13.0（已封版），迁移 000001–000042，无正式 v14 计划
- 分支 djw-workbuddy，工作区干净；最新进度 v0.13.36-dev（帖子元信息、系统 Logo 品牌管理、中文头像错误、仓库 Skill 直用）
- 架构：Go + Vue 3 模块化单体，一个 API 进程 + 三前端（web:3000 / admin:3001 / docs-site:3002 / API:8080）
- 四类扩展边界：Core Module（常开）/ Built-in Feature（编译内置可启停）/ External Plugin（可安装治理）/ Resource Package（纯数据）
- 验证命令：Go `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1`；web `pnpm lint && pnpm format:check && pnpm build`；admin `pnpm test:component && pnpm build`；docs `make readme-check && make docs-links && make architecture-check`
- 任务路由 skill：campusos-dev-nocommit / campusos-dev-workflow / campusos-docker-development / campusos-webui-regression / campusos-data-architecture-sync / campusos-readme-update
- 后续候选（非承诺）：P0 生产交付认证、文档与发布治理、安全运维闭环；P1 标准 MCP、真实 gRPC、远程插件目录等

## CI 修复：Shell 脚本缺 +x 权限位

CI Backend Test `make database-check` → `v13-migration-check` 失败：`./scripts/test-v13-schema-index-hygiene-migration.sh: Permission denied`（Error 127）。根因：脚本在 Windows 创建，git mode 100644，而 Makefile 惯例以 `./scripts/...` 直接执行。

- 排查全仓库 6 个 100644 的 .sh（3 个 scripts/ + 3 个 skills/ 下），全部 `git update-index --chmod=+x` 修复
- 验证：bash -n ×6、git diff --check、check-line-endings 全过
- 按 campusos-dev-nocommit 惯例写进度文档 `docs/进度/v0.13-dev/v0.13.37-dev.md`，未提交未推送，等用户确认后提交
- 经验：Windows 新建 .sh 脚本提交前需确认 git 权限位为 100755
