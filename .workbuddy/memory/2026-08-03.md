# 2026-08-03

## CampusOS 项目 Onboarding

使用 `skills/sources/campusos-project-onboarding` skill 完成项目快速了解：

- 当前基线：v0.13.0（已封版），迁移 000001–000042，无正式 v14 计划
- 分支 djw-workbuddy，工作区干净；最新进度 v0.13.36-dev（帖子元信息、系统 Logo 品牌管理、中文头像错误、仓库 Skill 直用）
- 架构：Go + Vue 3 模块化单体，一个 API 进程 + 三前端（web:3000 / admin:3001 / docs-site:3002 / API:8080）
- 四类扩展边界：Core Module（常开）/ Built-in Feature（编译内置可启停）/ External Plugin（可安装治理）/ Resource Package（纯数据）
- 验证命令：Go `GOCACHE=/tmp/campusos-go-cache go test ./... -count=1`；web `pnpm lint && pnpm format:check && pnpm build`；admin `pnpm test:component && pnpm build`；docs `make readme-check && make docs-links && make architecture-check`
- 任务路由 skill：campusos-dev-nocommit / campusos-dev-workflow / campusos-docker-development / campusos-webui-regression / campusos-data-architecture-sync / campusos-readme-update
- 后续候选（非承诺）：P0 生产交付认证、文档与发布治理、安全运维闭环；P1 标准 MCP、真实 gRPC、远程插件目录等

## CI 修复：Shell 脚本缺 +x 权限位

CI Backend Test `make database-check` → `v13-migration-check` 失败：`./scripts/test-v13-schema-index-hygiene-migration.sh: Permission denied`（Error 127）。根因：脚本在 Windows 创建，git mode 100644，而 Makefile 惯例以 `./scripts/...` 直接执行。

- 排查全仓库 6 个 100644 的 .sh（3 个 scripts/ + 3 个 skills/ 下），全部 `git update-index --chmod=+x` 修复
- 验证：bash -n ×6、git diff --check、check-line-endings 全过
- 按 campusos-dev-nocommit 惯例写进度文档 `docs/进度/v0.13-dev/v0.13.37-dev.md`，未提交未推送，等用户确认后提交
- 经验：Windows 新建 .sh 脚本提交前需确认 git 权限位为 100755

## CI 修复 2：Skill 脚本旧路径

权限位修复被用户提交（f3d052c）并推送后，CI `architecture-check` 报新错：Makefile:77 引用 `skills/campusos-data-architecture-sync/scripts/check_architecture_sync.py` 不存在。根因：v0.13.36 把 Skill 迁到 `skills/sources/` 后 Makefile 与 release-check.sh 未同步。

- 修复 4 处活引用：Makefile:67/68（readme-check）、Makefile:77（architecture-check）、scripts/release-check.sh:60，全部指向 `skills/sources/`
- 历史进度文档中的旧命令记录按项目维护原则不改写
- 验证：architecture-check 4 条命令、readme-check 2 条命令逐条实跑全过（本机无 make）；docs-links、bash -n、diff --check、行尾检查全过
- 进度文档 `docs/进度/v0.13-dev/v0.13.38-dev.md`，未提交未推送

## 功能：帖子列表行板块/标签（v0.13.39-dev）

- 实现：列表 payload 已含 category_id/tags，零 API 改动；新增 useCategoryCatalog（tree 一次加载 + Map，避免 N+1）、threadTags.ts（共享 normalize）、ThreadBoardChips.vue；ThreadListView 移动/桌面双布局接入；ThreadTaxonomy 重构复用共享工具
- 关键约束：全部帖子/分组/板块筛选共用 ThreadListView 一个组件；tags 可能为 null；板块目录会话级缓存
- 验证：Docker web 容器内 pnpm test:component（21 过，新增 4 用例）、lint、format:check、build（vue-tsc）全过；docs-site build 过；运行时 curl 确认字段存在
- 环境：本机无 make/pnpm/node_modules，web/docs 验证一律在 campusos-dev-web-1 / campusos-dev-docs-1 容器内执行（工作目录 /workspace/web、/workspace/docs-site）
- 文档同步：Help v13WebUI 说明 + docs-site web-ui-storage-notifications.md
- 未提交未推送

## 功能：评论治理删除与版主授权通知（v0.13.40-dev）

- 新类型：`community.post.deleted_by_moderator`（可靠命令 community.post.admin_delete，失败即回滚）、`identity.moderator.granted`/`.revoked`（授权提交后尽力而为投递，差额 granted/revoked，幂等不重复，自我抑制）
- 架构：新 Port `community.notification-writer`（ModerationNotifier），moderation 模块消费；DeletePostForModeration 透传 actorID；AdminDeletePost 用 reliability.Execute 包裹 + 新事件 eventbus.EventPostDeleted
- Web 零改动（通知全由服务端渲染）；DB 无迁移（type 为 VARCHAR(64) 自由文本）
- 测试：5 个新增，全过；architecture-check、projectaudit 合同测试过
- 全量 go test 有 3 组既有 Windows 宿主失败（0600 文件模式、grpc-example 产物缺失、reliability 时序），已用 `git worktree add <dir> HEAD` 在干净副本上复跑确认与改动无关（比 stash 安全）

## 事故：git stash push -u 导致 .git 损坏（已完全恢复）

- 经过：为对比既有测试失败执行 `git stash push -u`，操作中途失败并清空了 `.git/refs/` 与全部 loose objects（疑似 Windows + Docker 绑定挂载环境下 stash 中途崩溃）
- 恢复：重建 `.git/refs/heads|tags` → 从 `.git/logs/HEAD` reflog 找到分支 tip（1a672d29）→ 写回 refs/heads/djw-workbuddy → `git fetch origin` 恢复对象（用户已推送两个本地提交，远端是最新）
- 教训：**本仓库禁止 `git stash push -u`**（Docker 容器实时读写工作区）；验证既有失败用 `git worktree add <临时目录> HEAD`；GOCACHE 放 `/.cache/`（已 gitignore）；reflog（.git/logs）是本地提交恢复的命脉
- 用户两次提交 f3d052c、1a672d29 已推送 origin/djw-workbuddy；本轮所有改动（通知功能+进度文档）仍未提交
