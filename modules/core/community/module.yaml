schema: campusos.module/v1
id: core.community
name: community
display_name: "Community"
version: "0.12.1"
description: "Categories, tags, threads, posts, safe content rendering, publication state, and public content policy."
kind: core
activation_mode: always-on
default_enabled: true
dependencies: [core.event-bus, core.identity, core.feature-registry, core.reliability]
implementation: internal/modules/core/community
