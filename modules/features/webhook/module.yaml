schema: campusos.module/v1
id: feature.webhook
name: webhook
display_name: "Webhook"
version: "0.11.0"
description: "Managed webhook endpoints, delivery signatures, retries, and delivery audit."
kind: builtin-feature
activation_mode: restart
default_enabled: true
dependencies: [core.event-bus, core.reliability]
implementation: internal/modules/features/webhook
